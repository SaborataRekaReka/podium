(() => {
  "use strict";
  const prm = window.matchMedia("(prefers-reduced-motion: reduce)");
  const rdm = () => prm.matches;
  const C = {
    hero: { title: "На подиуме шины Cordiant", subtitle: "Выкатываются 3 шины", lead: "Scroll-story о бренде, летней линейке, технологиях и инженерной базе Cordiant с акцентом на российские условия эксплуатации." },
    brand: {
      intro: "Торговая марка Cordiant — это ведущий российский производитель шин, работающий в составе холдинга АО «Кордиант», одного из крупнейших в Центральной и Восточной Европе. Производство шин под брендом Cordiant началось в 2005 году на базе старейших шинных предприятий страны — Ярославского и Омского шинных заводов. Сегодня под брендами Cordiant и Сordiant Professional производится резина для легковых автомобилей и коммерческого транспорта — всего свыше 400 моделей. Для этого задействованы заводы шинного кластера в Омске, Ярославле и Ульяновске. Ассортимент охватывает все основные сегменты — от легковых до грузовых шин с фокусом на соответствие российским условиям эксплуатации. Подтверждением высокого качества резины Cordiant является то, что она поставляется для заводской комплектации автомобилей, сходящих с конвейеров компаний УАЗ, КАМАЗ, ГАЗ, а также ряда производителей спецтехники. Продукция бренда реализуется через сеть дистрибьюторов, насчитывающую более 300 партнеров по всей России.", symbolism: "Логотип Cordiant изначально включал название бренда и след от лапки геккона — ящерицы, которая умеет перемещаться по стенам и потолкам. Этот образ символизирует цепкость производимых шин. В 2023 году в рамках продвижения бренда и улучшения визуализации и узнаваемости логотип был модифицирован. Символизировавшая бренд лапка геккона была заменена стилизованным изображением самой ящерицы. Акцент с лапки на фигурку геккона сделало логотип более ярким и запоминающимся. Полоса, идущая по спине ящерицы через весь логотип, добавляет динамики. Кроме геккона, Cordiant использует в рекламе образы акулы и барса, что символизирует естественное сцепление шин с мокрыми и заснеженными дорогами.", testing: "Вся новая продукция марки, включая прототипы, в которые нужно внести изменения, проходит многоуровневые испытания. Сначала проводятся стендовые тесты в лаборатории, а затем шины отправляются на полигон для дорожных испытаний. В распоряжении кластера компаний «Кордиант» имеется сразу несколько полигонов в различных географических широтах: шинный испытательный центр «Вершина», полигон НАМИ и испытательный полигон Северо-Восточного федерального университета в ТОР «Якутия». Cordiant также использует внешние испытательные центры, например международные лаборатории (DEKRA, IDIADA, DUK Test), для проведения сертификационных и сравнительных испытаний. Сотрудничество полигонов и лабораторий позволяет обеспечивать качество продукции в разных условиях эксплуатации.", rnd: "НТЦ «Интайр» — это уникальное научное подразделение кластера «Кордиант». Центральный офис находится в Москве, на производственных площадках в Калуге, Ульяновске, Омске и Ярославле действуют подразделения НТЦ. Портфолио работ НТЦ включает компьютерное моделирование и расчеты шин, разработку дизайна и конструкции, создание новых резин, омологацию нового сырья и материалов, а также инжиниринг технологических процессов. Центр сотрудничает с ведущими мировыми институтами и испытательными полигонами, включая полигон «Вершина» в Ярославле, где тестируются все типы шин — от легковых до авиационных. Вся разрабатываемая шинная продукция проходит дорожные, стендовые и сертификационные испытания. Наличие собственного R\u0026D-центра с высокой экспертизой в разработке продукции, омологации сырья и сопровождение испытаний позволяет быстро развивать ассортимент выпускаемой продукции.", lab: "Массовое производство шин требует безупречного качества поступающего сырья и материалов. В шинном кластере «Кордиант» каждая единица сырья проходит множество тестов в лабораториях, которые расположены во всех региональных подразделениях холдинга. Здесь проводятся испытания по более чем 400 методам, включая тестирование компаунда и элементов каркаса в соответствии с ASTM, ISO, ГОСТ и внутренними методиками. Также проводятся исследования свойств новых рецептур, испытания новых режимов смешения и вулканизации, а также приготовление шинных срезов. Лаборатории разрабатывают собственные резиновые смеси и проводят физико-механические испытания и различные тесты, чтобы убедиться, что они соответствуют высоким требованиям качества компании без каких-либо компромиссов.", advantages: "Согласно тестам ведущих шинных экспертов и автомобильных изданий, шины Cordiant показывают характеристики схожие, а в некоторых случаях и превосходящие конкурентов премиум-сегмента. Продукция марки пользуется стабильно высоким спросом прежде всего благодаря передовым технологиям производства. Высокие экологические характеристики достигаются за счет использования шинных смесей последнего поколения. Устойчивость к аквапланированию, курсовая устойчивость и эффективность торможения достигаются за счет компьютерного моделирования протектора в собственном НТЦ «Интайр». Надежность конструкции обеспечивается современными технологиями укрепления каркаса и инновационной системой укладки слоев компаунда. Слагаемые высокой ходимости — инновационные смеси и оптимизированная структура рисунка протектора. На амортизирующие возможности работает облегченный материал корда и эластичные боковины. В то же время продукция бренда отличается ценовой доступностью. Не уступая по уровню комфорта и безопасности известным западным брендам, покрышки Cordiant оказываются значительно дешевле зарубежных аналогов.",
      timeline: [
        { year: "1995", text: "На базе Ярославского и Омского шинных заводов создано АО «Кордиант»." },
        { year: "2005", text: "Запущен бренд Cordiant; первые модели — Cordiant Sport и Cordiant Polar." },
        { year: "2009", text: "Создан НТЦ «Интайр» (Ярославль)." },
        { year: "2012", text: "Компания «СИБУР — Русские шины» становится самостоятельной организацией «Кордиант»." },
        { year: "2023", text: "Группа S8 Capital приобретает 100% акций АО «Кордиант»; обновляется логотип." },
        { year: "Октябрь 2024", text: "Собственником становится «Севергрупп»." },
        { year: "География", text: "Производство в Ярославле, Омске, Ульяновске и Калуге." }
      ]
    },
    summerOverview: "Актуальная линейка летних легковых шин Cordiant представлена девятью моделями, каждая из которых разработана для определенных условий эксплуатации и стиля вождения. Модели Cordiant Gravity, Cordiant Gravity SUV, Cordiant Run Tour, Cordiant Comfort 2, Cordiant Sport 3, Cordiant Road Runner предназначены для легковых автомобилей, Cordiant Off Road, Cordiant Off Road 2, Cordiant All Terrain отличает широкая адаптация под климатические и дорожные условия России. Как следствие, в широком модельном ряду шин Cordiant каждый клиент может найти шины, подходящие как для его стиля вождения, так и для различных условий эксплуатации. Повышенным спросом пользуются такие новинки, как Cordiant Run Tour, Cordiant Gravity, Gravity SUV (2023 год) и Cordiant Comfort 2.",
    allModels: [
      { key: "runTour", name: "Run Tour", image: "assets/img/tires/cordiant-run-tour.png", tags: ["Город", "Трасса", "Тишина"] },
      { key: "comfort2", name: "Comfort 2", image: "assets/img/tires/cordiant-comfort-2.png", tags: ["Комфорт", "Мокрая дорога"] },
      { key: "sport3", name: "Sport 3", image: "assets/img/tires/cordiant-sport-3.png", tags: ["Динамика"] },
      { key: "offRoad", name: "Off Road", image: "assets/img/tires/cordiant-off-road.png", tags: ["Внедорожье"] },
      { key: "offRoad2", name: "Off Road 2", image: "assets/img/tires/cordiant-off-road-2.png", tags: ["Грунт"] },
      { key: "gravity", name: "Gravity", image: "assets/img/tires/cordiant-gravity.png", tags: ["Асфальт + гравий"] },
      { key: "gravitySuv", name: "Gravity SUV", image: "assets/img/tires/cordiant-gravity-suv.png", tags: ["SUV", "Кроссоверы"] },
      { key: "allTerrain", name: "All Terrain", image: "assets/img/tires/cordiant-all-terrain.png", tags: ["Смешанные покрытия"] },
      { key: "roadRunner", name: "Road Runner", image: "assets/img/tires/cordiant-road-runner.png", tags: ["Ежедневная езда"] }
    ],
    summerFeatured: {
      runTour: { title: "Cordiant Run Tour", image: "assets/img/tires/cordiant-run-tour.png", tags: ["Город", "Трасса", "Тишина", "Экономичность"], summary: "Шины Run Tour ориентированы на комфортную езду по шоссе и в городе с акцентом на плавность хода, низкий шум и экономичность. Оптимизированный состав резиновой смеси  ряд технических решений, позволили снизить индекс сопротивления качению и экономить до 30% топлива по сравнению с шиной-предшественником. Нейлоновый бандаж в конструкции обеспечивает защиту от проколов. Усиленный металлический корд повышает износостойкость. Двойной слой резины в боковине защищает от порезов и повреждений при наезде на ямы и прочие неровности. Благодаря применению технологии Variable Pitch Optimization оптимизированный рисунок протектора минимизирует воздушные завихрения и резонансные частоты, за счет чего шины остаются тихими даже на скорости 150 км/ч. В то же время эта шина отличается хорошей управляемостью. Оптимизированный рисунок протектора с широкими продольными ребрами и массивными плечевыми блоками обеспечивает точное и предсказуемое поведение автомобиля на сухом асфальте. На мокрой же дороге риск аквапланирования снижает эффективная дренажная система. Модель оценят водители, которые ездят преимущественно по ровным дорогам (город, трасса), ценят комфорт, тишину и топливную экономичность." },
      gravity: { title: "Cordiant Gravity", image: "assets/img/tires/cordiant-gravity.png", tags: ["Живучесть", "Гравий", "LONG-COR", "69 дБ"], summary: "Модель Cordiant Gravity предназначена для комфортной и безопасной езды по асфальту разного качества и гравийному покрытию с акцентом на износостойкость и долговечность в условиях российских дорог. В протекторе шины использована оптимизированная смесь с большим содержанием высокоактивного техуглерода, что увеличивает ходимость без ущерба для других свойств шины. Специфический рисунок протектора без больших разрезов и пустот предотвращает забивание мелких камней, защищает от повреждений на гравийных дорогах. Технология LONG-COR за счет увеличенной глубины протектора (на 10–12% больше, чем у многих аналогов) и оптимизированного профиля увеличивает срок службы шины на сезон по сравнению с конкурентами. Сочетание технологий DRY-COR и WET-COR обеспечивает отличную управляемость, в поворотах и при перестроении, а также надежное сцепление с мокрой дорогой и эффективную борьбу с аквапланированием даже на высоких скоростях. При этом Cordiant Gravity обеспечивает акустический комфорт и плавность хода в городе и на скоростной магистрали. Уровень шума составляет около 69 дБ, что сопоставимо с премиальными моделями. Шина ориентирована на водителей, которые часто ездят по смешанному покрытию (асфальт + гравий), ценят долгий срок службы и готовы пожертвовать максимальной тишиной ради живучести." },
      gravitySuv: { title: "Cordiant Gravity SUV", image: "assets/img/tires/cordiant-gravity-suv.png", tags: ["SUV", "STONE-COR", "Wet-Cor", "Гравий"], summary: "Cordiant Gravity SUV ориентирована на владельцев кроссоверов, внедорожников и крупных минивэнов. Обладая теми же характеристиками, что и легковая версия, эта модель адаптирована для эксплуатации на гравийных дорогах. Основа каркаса ориентирована на использование на внедорожниках и других автомобилях с высоким центром масс. Такой конструктив позволяет шинам выдерживать растягивающие, разрывающие, ударные и прочие механические воздействия. Жесткие продольные ребра в центральной секции обеспечивают высокую курсовую устойчивость. При помощи двух центральных ребер со скосами увеличивается количество граней зацепления при движении по грунтовкам. На дне канавки расположен камневыталкивающий шип для быстрого самоочищения от камней при движении по гравийной дороге (технология Stone-Cor). Особенностью SUV-версии также являются увеличенные прорези в плечевых зонах для лучшего отвода воды из пятна контакта. Улучшенная дренажная система с продольными, диагональными и поперечными канавками эффективно отводит воду, защищая от аквапланирования даже при интенсивных осадках (технология Wet-Cor). Также в составе резиновой смеси здесь используется кремнезем нового поколения, высокомолекулярные полимеры и технический углерод. Это обеспечивает стойкость к истиранию, повышает коэффициент сцепления, снижает сопротивление качению. Модель при этом подходит для эксплуатации и в городских условиях, и на скоростном шоссе." },
      comfort2: { title: "Cordiant Comfort 2", image: "assets/img/tires/cordiant-comfort-2.png", tags: ["Wet-Cor", "Flex-Cor", "Dry-Cor", "37 типоразмеров"], summary: "Шины Cordiant Comfort 2 разрабатывалась с прицелом на комфортную и безопасную езду. Такие шины хорошо сглаживают неровности, снижают уровень шума в салоне, устойчивы к аквапланированию и уверенно тормозят на сухом и мокром асфальте. Благодаря модернизированному компаунду и пересмотренной геометрии протектора в модели была существенно увеличена площадь пятна контакта беговой части протекторного рисунка с дорогой. Как следствие улучшились характеристики торможения на сухом асфальте. В дождь и слякоть в этих шинах работает фирменная технология Wet‑Cor – передовая система дренажа для борьбы с аквапланированием – дуговые канавки и ребра разной формы эффективно отводят воду из пятна контакта. Эластичные боковины и облегченный каркас, усиливают амортизацию и снижают ударную нагрузку на подвеску (технология FLEX-COR). Курсовую устойчивость при движении по прямой обеспечивают три сплошных ребра, расположенные на беговой дорожке, а внешняя сторона протектора, состоящая из крупных шашек, объединенных общим ребром жесткости гарантирует устойчивость при маневрах и в поворотах (технология DRY-COR). В свою очередь, асимметричный протектор с 12 неповторяющимися элементами и насечкой «Антишум» повышает акустический комфорт. Внешний шум Cordiant Comfort 2 составил лишь 71 дБ, что на уровне европейских конкурентов. Стоимость этой модели при этом на 30% ниже, чем у премиальных аналогов (например, Bridgestone Turanza или Continental ContiSportContact). Шины выпускаются в 37 типоразмерах: от 175/70 R13 (для классических легковых авто) до 265/60 R18 (для крупных кроссоверов и внедорожников)." }
    },
    tech: { stages: "Производство шин Cordiant включает несколько ключевых этапов. Все начинается с того, что инженеры научно-технического центра «Интайр» создают виртуальные версии будущих шин, включая дизайн протектора, боковин и другие параметры. Резиновые смеси готовятся в миксерах с точным дозированием компонентов: натурального и синтетического каучука, технического углерода (сажи), серы, кремнезема и других присадок. Готовые смеси проходят автоматический контроль качества. Из резиновых смесей изготавливают отдельные детали: обрезиненный корд, каркас, брекер, боковины, протектор. Лента резины затем экструдируется в нужную форму с учетом дизайна протектора. На стадии сборки составляющие шины соединяются вместе. Затем в процессе вулканизации, который проходит при температуре почти 170°C и давлении около 16,5 бара, шина приобретает окончательную форму и физико-механические свойства. Каждая выпущенная шина проходит контроль качества, часть партии отправляют на рентген-установку. После успешного контроля качества шины могут подвергаться дополнительной обработке (например, удалению лишних резиновых «волосков» на поверхности протектора) и упаковываются для отправки на склад или в продажу.", sites: "Производство легковых шин Cordiant на заводах в Омске, Ульяновске и Ярославле имеет свои особенности, связанные с историей предприятий, технологиями, специализацией и производственными мощностями. Завод «Кордиант-Восток» специализируется на выпуске легковых и легкогрузовых шин, а также шин для внедорожников под брендами Cordiant и Tunga. Производственная мощность — более 4 млн шин в год. На предприятии трудится более 850 квалифицированных рабочих и специалистов. Завод Gislaved в Ульяновске вошел в структуру холдинга «Кордиант» в 2024 году и был переименован. Это одно из самых современных шинных предприятий полного цикла в России. Здесь выпускают зимние и летние модели шин Cordiant (средний ценовой сегмент) и Gislaved (премиальный сегмент) для легковых автомобилей, кроссоверов и внедорожников c большим разбросом типоразмеров.", staff: "Квалифицированные специалисты, система программ подготовки кадров, сетевой обмен знаниями, современное оборудование и высокий уровень автоматизации производства являются ключевыми факторами успеха калужского шинного предприятия. В рамках шинного кластера «Кордиант» действует единый научно-технического центр разработки продукции, объединивший технологов, ранее работавших на конкурирующие бренды. Холдинг сотрудничает с ведущими российскими университетами в рамках программ целевой подготовки кадров. Все это позволяет индустриализировать новые модели шин быстро и качественно.", quality: "Холдинг «Кордиант» уделяет большое внимание качеству и безопасности продукции. Компания применяет многоступенчатый процесс проверки. В 2024 году холдинг анонсировал внедрение системы контроля качества на основе искусственного интеллекта. Технология компьютерного зрения позволяет автоматически выявлять потенциальные дефекты в шинах и группировать их по различным критериям. Команда Cordiant выпускает продукцию высочайшего качества с минимальным количеством отходов, бережным отношением к энергоресурсам, эффективным расходом материалов и соблюдая требования безопасности труда. Все шины, которые сходят с конвейера завода, отвечают стандартам качества ISO 9001:2015. В январе 2026 года эксперты Роскачества подтвердили экологическую безопасность шин Cordiant в ходе независимых испытаний.", strategyMain: "На трех производственных площадках выпускаются шины Cordiant диаметром от 13 до 18 дюймов для легковых и легкогрузовых автомобилей. Стратегия кластера «Кордиант» предусматривает в ближайшей перспективе выпуск свыше 1000 SKU в самых распространенных типоразмерах (с посадочным диаметром 13–22 дюйма в легковом классе и 17,5–22,5 — в грузовом) и присутствие во всех рыночных сегментах.", strategyExtra: "В августе 2025 года холдинг представил линейку региональных грузовых шин Cordiant Professional. Она включает три модели: FR-2 — для рулевых осей, DR-2 — для ведущих осей и TR-3 — для прицепных осей. Все модели выпускаются в наиболее востребованных типоразмерах с посадочным диаметром 22,5 дюйма.", development: "Бренд Cordiant активно развивает технологии в производстве шин, внедряя инновационные решения для повышения износостойкости, управляемости, безопасности и топливной экономичности. В 2024 году Cordiant заключил соглашение с компанией «СИБУР» о сотрудничестве в области разработки новых синтетических видов каучука, что должно улучшить свойства шин и способствовать импортозамещению. Инженеры НТЦ «Интайр» работают над созданием новых рецептур резиновых смесей, используя собственные наработки, а также технологии, унаследованные от бывших владельцев заводов (Continental и Bridgestone). Это включает разработку смесей с повышенной однородностью материала, что улучшает качество шин. Продолжается модернизация производственных мощностей, включая внедрение автоматизированных систем, расширяется сотрудничество с вузами и институтами. Совместно со специалистами ИРНИТУ разрабатывается уникальный стенд для измерения силы трения." },
    russia: {
      intro: "Шины Cordiant проектируются с учетом российских реалий. Не всегда хорошее состояние дорог (ямы, выбоины, неровное покрытие, грунтовки), большие расстояния (необходимость низкого сопротивления качению для экономии топлива), резкие перепады температур (оттепели, заморозки, переход через 0°C), а также разнообразие климатических зон (от субтропиков Сочи до вечной мерзлоты Якутии) - все это учитывается при разработке новинок бренда.", details: "Использование передовых технологий выделяет шины Cordiant на рынке. В составе резиновой смеси используются силикон-содержащие полимеры для улучшения характеристик сцепления на мокрой и заснеженной дороге. Для повышения износостойкости и увеличения срока службы шин включаются карбонатные добавки. Рисунок протектора оптимизируется для различных погодных условий и типов дорожного покрытия. Использование асимметричного и направленного дизайна протектора задействуют для улучшения управляемости и стабильности при высоких скоростях. Еще один пример адаптации - применение стальных кордов и специализированных текстильных материалов для укрепления боковин и предотвращения их порезов и разрывов. В свою очередь внедрение инновационной системы укладки слоев увеличивает общую прочность и долговечность покрышек. Еще одно важное направление - следование экологическим стандартам. Речь идет прежде всего о снижении содержания вредных веществ в составе резиновой смеси. Также применятся технологии, позволяющие уменьшить сопротивление качению, что приводит к сокращению расхода топлива и выделения углекислого газа.",
      logic: [
        { condition: "Ямы, выбоины, грунтовки и тяжелые покрытия", solution: "Стальные корды, спецтекстиль, укрепленные боковины и инновационная укладка слоев.", benefit: "Выше прочность и меньше риск повреждений на плохих дорогах." },
        { condition: "Резкие перепады температур и разные климатические зоны", solution: "Силикон-содержащие полимеры и рецептуры под широкий температурный диапазон.", benefit: "Стабильное сцепление и предсказуемая управляемость." },
        { condition: "Длинные дистанции и требование к экономичности", solution: "Карбонатные добавки и оптимизация рисунка протектора для снижения сопротивления качению.", benefit: "Ниже расход топлива и выбросы CO2 при сохранении ресурса." }
      ]
    },
    materials: {
      intro: "Для изготовления шин Cordiant используются самые передовые материалы и компоненты. Резиновая смесь летних шин создается на основе силикон-содержащих полимеров, которые улучшают сцепление с мокрой дорогой. В некоторых моделях используется ультрасовременный каучук DSSK - он снижает расход топлива и повышает сцепные качества, а также гарантирует курсовую устойчивость на скоростях до 210 км/ч (индекс скорости H). Карбонатные добавки повышают износостойкость протектора, способствуют курсовой устойчивости и продлевают срок службы шин. Кремнезем (силика, SiO2) оптимизирует сцепление на слегка увлажненном асфальте и способствует снижению сопротивления качению. Компоненты с пониженным содержанием вредных веществ обеспечивают соответствие экологическим стандартам. Такой подход позволяет Cordiant предлагать летние шины с четким разделением по характеристикам - от экономичных до высокопроизводительных, при этом все модели сохраняют устойчивость к российским дорожным и погодным условиям.",
      components: [
        { title: "Силикон-содержащие полимеры", what: "Основа летнего компаунда для мокрой дороги.", effect: "Улучшают сцепление и стабильность в дождь." },
        { title: "Каучук DSSK", what: "Современный материал в части моделей.", effect: "Снижает расход топлива и поддерживает устойчивость до 210 км/ч." },
        { title: "Карбонатные добавки", what: "Компоненты повышения ресурса протектора.", effect: "Увеличивают износостойкость и срок службы." },
        { title: "Кремнезем (silica, SiO2)", what: "Активная добавка для сцепления и энергоэффективности.", effect: "Снижает сопротивление качению и улучшает поведение на влажном асфальте." }
      ]
    },
    modelTech: { runTour: "Cordiant Run Tour сочетает технологии, нацеленные на три ключевых аспекта: безопасность, комфорт и экономичность. В Cordiant Run Tour применен корд сверхвысокой прочности ST (Super high Tensile), повышающий ударостойкость на неровных дорогах, и расширенные слои каркаса. Два слоя резины в боковине обеспечивают дополнительную защиту от боковых порезов и ударов при наезде на бордюры или выбоины. За защиту протекторной области от проколов отвечает усиленный нейлоновый бандаж. Симметрично направленный рисунок обеспечивает стабильность на прямой и предсказуемое поведение в поворотах. Диагональные канавки, расположенные под острым углом к направлению движения, уменьшают деформацию протектора и сокращают длину тормозного пути на мокрой дороге. Глубокие и широкие дренажные каналы эффективно отводят воду из пятна контакта, снижая риск аквапланирования. Благодаря асимметричному расположению элементов протектора (технология Variable Pitch Optimization) минимизируются воздушные завихрения, что снижает уровень шума даже на высокой скорости. Модель выпускается в диаметрах 13–17 дюймов, подходит для компактных городских авто и среднеразмерных седанов.", gravity: "Технология DRY-COR, примененная в Cordiant Gravity, улучшает характеристики на сухих дорогах. Жесткие шашечные элементы плечевых ребер не дают шине деформироваться в повороте, сохраняя стабильность пятна контакта и сцепления с дорогой, предотвращая сносы и заносы. При этом разноразмерные шашки протектора расположены так, чтобы исключить звуковой резонанс. Уровень шума снижен до 69 дБ. На двух центральных ребрах предусмотрены скосы, за счет которых увеличивается количество граней зацепления при движении на мягких грунтах и грунтовой дороге. Такое ноу-хау, как LONG-COR, работает на износоустойчивость резинового состава без ущерба остальным характеристикам. Долговечность повышена за счет увеличения глубины протектора на 12%. Технология WET-COR, представленная широкими водоотводными канавками-водоотводами, отвечает за поведение на мокрой дороге.", gravitySuv: " Конструктивно модель Cordiant Gravity SUV во многом повторяет Cordiant Gravity. Эта шина разработана специально для кроссоверов и внедорожников и адаптирована для российских условий эксплуатации, включая городские дороги и проселочные пути. Ее отличительная особенность — технология STONE-COR. За счет скосов на двух центральных ребрах увеличено количество граней зацепления при движении на мягких грунтах и гравийных дорогах. Центральные канавки протектора оборудованы специальными камневыталкивающими шипами для быстрого самоочищения протектора от камней. Асимметричный ненаправленный протектор обеспечивает эффективное сцепление с дорогой. Усиленный силовой каркас адаптирован для работы в сложных условиях, позволяет выдерживать значительные механические нагрузки на разрыв и растяжение.", comfort2: "Комфорт, хорошая управляемость и безопасность езды поддерживаются в Cordiant Comfort 2 несколькими фирменными ноу-хау. Система Wet‑Cor подразумевает продуманный дренаж для борьбы с аквапланированием. Особые канавки и ребра разной формы эффективно отводят воду из пятна контакта. Существенную роль играет также новая резиновая смесь с большим содержанием силики, которая позволила повысить сцепные свойства шины на мокрой поверхности, одновременно снизив коэффициент сопротивления качению. Система Flex‑Cor подразумевает эластичные боковины, усиливающие амортизацию и снижающие ударную нагрузку на подвеску. На комфорт работает также инновационная методика чередования особых элементов протектора. Его асимметричный рисунок создан путем комбинирования 12 уникальных элементов - так на скорости снижается вероятность появления резонансных колебаний и дополнительных шумов. Технология Dry‑Cor включает в себя три сплошных ребра протектора для курсовой устойчивости на прямой. Устойчивость в поворотах и при перестроении обеспечивает внешняя сторона протектора, состоящая из крупных шашек, объединенных общим ребром жесткости." },
    reasons: [
      {
        title: "Качественный продукт",
        text: "Компания Cordiant фокусируется на массовом производстве и при этом делает ставку на самые передовые технологии, обеспечивающие безопасность, комфорт и износостойкость. Продукция бренда сертифицирована по ключевым международным и национальным стандартам. На заводах компании применяется высокотехнологичное оборудование, задействован высококвалифицированный персонал. Внедрена система компьютерного контроля на всех этапах производства, готовая продукция проходит строгий контроль в реальных условиях эксплуатации в различных климатических зонах.",
        image: "assets/img/hero/%D0%9F%D0%BE%D0%B4%D0%B8%D1%83%D0%BC%20%D0%9A%D0%BE%D1%80%D0%B4%D0%B8%D0%B0%D0%BD%D1%82%202026/6%20%D0%B1%D0%BB%D0%BE%D0%BA/1.jpg",
        video: "assets/img/features_video/1.mp4",
        variant: "photo"
      },
      {
        title: "Созданы для России",
        text: "Бренд Cordiant ориентирован на производство шин, с учетом российских реалий - от сурового климата до особенностей дорожного покрытия. Шины Cordiant рассчитаны на эксплуатацию в широком диапазоне температур. Зимние модели сохраняют эластичность при морозах до −40°C и ниже за счет специальных резиновых смесей, летние шины устойчивы к высоким температурам и не «плывут» на разогретом асфальте благодаря компаунду.  Многие модели, такие как Gravity, имеют усиленный протектор с увеличенной глубиной и оптимизированным профилем, что повышает износостойкость и позволяет уверенно передвигаться по разбитому асфальту, гравию и даже легкому бездорожью.",
        image: "assets/img/hero/%D0%9F%D0%BE%D0%B4%D0%B8%D1%83%D0%BC%20%D0%9A%D0%BE%D1%80%D0%B4%D0%B8%D0%B0%D0%BD%D1%82%202026/6%20%D0%B1%D0%BB%D0%BE%D0%BA/4.jpg",
        video: "assets/img/features_video/2.mp4",
        variant: "photo"
      },
      {
        title: "Безопасность",
        text: "Безопасность в шинах Cordiant обеспечивается комплексом технологических решений, которые охватывают состав резиновой смеси, конструкцию протектора и укрепление каркаса. Применение стальных кордов, многослойной структуры увеличивает прочность и долговечность шин. Рисунок протектора способствует оптимальному распределению давления в пятне контакта и снижает риск деформации шины при маневрах. Различные решения аквадренажной системы эффективно предотвращают аквапланирование.",
        image: "assets/img/hero/%D0%9F%D0%BE%D0%B4%D0%B8%D1%83%D0%BC%20%D0%9A%D0%BE%D1%80%D0%B4%D0%B8%D0%B0%D0%BD%D1%82%202026/6%20%D0%B1%D0%BB%D0%BE%D0%BA/2.jpg",
        video: "assets/img/features_video/3.mp4",
        variant: "photo"
      },
      {
        title: "Долговечность",
        text: "Шины Cordiant обладают высокой ходимостью, что отвечает запросам практичных клиентов. Например,  модель Cordiant Gravity благодаря составу резиновой смеси с содержанием высокоактивного технического углерода и современных типов полимеров, оптимизированному профилю, обеспечивающему равномерное распределение давления в пятне контакта, и увеличенной глубине протектора на 12% отличается высокой износостойкостью и позволяет производить замену шин реже.",
        image: "assets/img/hero/%D0%9F%D0%BE%D0%B4%D0%B8%D1%83%D0%BC%20%D0%9A%D0%BE%D1%80%D0%B4%D0%B8%D0%B0%D0%BD%D1%82%202026/6%20%D0%B1%D0%BB%D0%BE%D0%BA/3.jpg",
        video: "assets/img/features_video/4.mp4",
        variant: "photo"
      },
      {
        title: "Сервисные программы",
        text: "Компания Cordiant предлагает несколько выгодных для клиентов сервисных программ, включая бесплатный шиномонтаж при покупке комплекта шин. Программа «Бесплатный шиномонтаж» действует при покупке комплекта легковых шин Cordiant (4 шины одного типоразмера). В услугу входит снятие и установка колес, демонтаж и монтаж шин, а также балансировка колес. Также существуют акции, которые могут включать дополнительные бонусы, например, кешбэк. Чтобы участвовать в акции, нужно купить одним чеком комплект из четырех шин Cordiant размерностью от 16 до 18 дюймов одной модели и одного типоразмера из выделенного ассортимента в период с 1 марта по 30 июня 2026 года включительно. Затем следует зарегистрироваться на cashback.cordiant.ru, загрузить чек в течение семи дней после покупки и получить баллы после проверки чека.",
        image: "assets/img/hero/%D0%9F%D0%BE%D0%B4%D0%B8%D1%83%D0%BC%20%D0%9A%D0%BE%D1%80%D0%B4%D0%B8%D0%B0%D0%BD%D1%82%202026/6%20%D0%B1%D0%BB%D0%BE%D0%BA/5.jpg",
        video: "assets/img/features_video/5.mp4",
        variant: "photo"
      }
    ]
  };
  const secIds = ["about", "summer", "technologies", "russia", "materials", "model-tech", "reasons"];
  const modelPhotoMap = {
    runTour: "assets/img/hero/Подиум Кордиант 2026/2 блок/RunTour.jpg",
    gravity: "assets/img/hero/Подиум Кордиант 2026/2 блок/Gravity.jpg",
    gravitySuv: "assets/img/hero/Подиум Кордиант 2026/2 блок/Gravity suv.jpg",
    comfort2: "assets/img/hero/Подиум Кордиант 2026/2 блок/Comfort2.jpg"
  };
  const summerCardPhotoFallback = "assets/img/hero/Подиум Кордиант 2026/5 блок/основная.jpg";
  const summerCardPhotoMap = {
    runTour: "assets/img/hero/Подиум Кордиант 2026/5 блок/RunTour.jpg",
    gravity: "assets/img/hero/Подиум Кордиант 2026/5 блок/Gravity.jpg",
    gravitySuv: "assets/img/hero/Подиум Кордиант 2026/5 блок/Gravity suv.jpg",
    comfort2: "assets/img/hero/Подиум Кордиант 2026/5 блок/Comfort2.jpg"
  };
  const summerFeatureMeta = {
    runTour: {
      scenario: "Город + трасса, упор на тишину и экономичность в ежедневном режиме.",
      benefits: ["Сниженное сопротивление качению и экономия топлива", "Точный отклик на сухом покрытии", "Стабильность и акустический комфорт на скоростях до 150 км/ч", "Усиленная конструкция для защиты от повреждений"],
      tech: ["Variable Pitch Optimization", "ST", "DRY-COR", "WET-COR"],
      image: "assets/img/hero/Подиум Кордиант 2026/2 блок/RunTour.jpg"
    },
    gravity: {
      scenario: "Смешанный маршрут: асфальт разного качества и гравийные участки.",
      benefits: ["Увеличенная ходимость за счет оптимизированной смеси", "Снижение риска повреждений на гравии", "Уверенное поведение в поворотах и при перестроении", "Низкий уровень шума около 69 дБ"],
      tech: ["LONG-COR", "DRY-COR", "WET-COR"],
      image: "assets/img/hero/Подиум Кордиант 2026/2 блок/Gravity.jpg"
    },
    gravitySuv: {
      scenario: "Кроссоверы и внедорожники: город, трасса и проселочные дороги.",
      benefits: ["Усиленный силовой каркас под высокий центр масс", "Эффективное самоочищение протектора от камней", "Стабильность на мягких грунтах и гравии", "Сохранение комфорта на асфальте"],
      tech: ["STONE-COR", "WET-COR", "DRY-COR"],
      image: "assets/img/hero/Подиум Кордиант 2026/2 блок/Gravity suv.jpg"
    },
    comfort2: {
      scenario: "Комфортная городская и междугородняя эксплуатация в широком диапазоне типоразмеров.",
      benefits: ["Плавный ход и сниженная вибрационная нагрузка", "Эффективная дренажная система против аквапланирования", "Курсовая устойчивость на скоростной прямой", "Сбалансированный уровень шума и торможения"],
      tech: ["WET-COR", "Flex-Cor", "DRY-COR"],
      image: "assets/img/hero/Подиум Кордиант 2026/2 блок/Comfort2.jpg"
    }
  };
  const modelTechMeta = {
    runTour: { tech: ["Variable Pitch Optimization", "ST", "WET-COR"], image: "assets/img/hero/Подиум Кордиант 2026/5 блок/RunTour.jpg" },
    gravity: { tech: ["DRY-COR", "WET-COR", "LONG-COR"], image: "assets/img/hero/Подиум Кордиант 2026/5 блок/Gravity.jpg" },
    gravitySuv: { tech: ["STONE-COR", "WET-COR", "DRY-COR"], image: "assets/img/hero/Подиум Кордиант 2026/5 блок/Gravity suv.jpg" },
    comfort2: { tech: ["WET-COR", "Flex-Cor", "DRY-COR"], image: "assets/img/hero/Подиум Кордиант 2026/5 блок/Comfort2.jpg" }
  };
  const modelLeads = {
    runTour: "Ключевая модель",
    gravity: "Ключевая модель",
    gravitySuv: "Ключевая модель",
    comfort2: "Ключевая модель",
    sport3: "Асфальтовая динамика",
    offRoad: "Классический оффроуд",
    offRoad2: "Грунтовый сценарий",
    allTerrain: "Смешанные покрытия",
    roadRunner: "Ежедневный городской режим"
  };
  const CORDIANT_ADAPT_HOTSPOTS = [
    {
      id: "roads",
      label: "Состояние дорог",
      tooltip: "ямы, выбоины, неровное покрытие, грунтовки"
    },
    {
      id: "distance",
      label: "Большие расстояния",
      tooltip: "необходимость низкого сопротивления качению для экономии топлива"
    },
    {
      id: "temperature",
      label: "Перепады температур",
      tooltip: "оттепели, заморозки, переход через 0°C"
    },
    {
      id: "climate",
      label: "Климатические зоны",
      tooltip: "от субтропиков Сочи до вечной мерзлоты Якутии"
    }
  ];
  const CORDIANT_TECH_SLIDES = [
    {
      key: "run-tour",
      title: "Cordiant Run Tour",
      text: "Cordiant Run Tour сочетает технологии, нацеленные на три ключевых аспекта: безопасность, комфорт и экономичность.",
      image: "assets/img/tires/cordiant-run-tour.png",
      watermark: "Run Tour"
    },
    {
      key: "gravity",
      title: "Cordiant Gravity",
      text: "Технология DRY-COR, примененная в Cordiant Gravity, улучшает характеристики на сухих дорогах.",
      image: "assets/img/tires/cordiant-gravity.png",
      watermark: "Gravity"
    },
    {
      key: "gravity-suv",
      title: "Cordiant Gravity SUV",
      text: "Конструктивно модель Cordiant Gravity SUV во многом повторяет Cordiant Gravity.",
      image: "assets/img/tires/cordiant-gravity-suv.png",
      watermark: "Gravity SUV"
    },
    {
      key: "comfort-2",
      title: "Cordiant Comfort 2",
      text: "Комфорт, хорошая управляемость и безопасность езды поддерживаются в Cordiant Comfort 2 несколькими фирменными ноу-хау.",
      image: "assets/img/tires/cordiant-comfort-2.png",
      watermark: "Comfort 2"
    }
  ];
  const CORDIANT_TECH_HOTSPOT_MODEL = {
    width: 470,
    height: 304,
    hub: { x: 286, y: 150 },
    items: [
      {
        id: "silica",
        label: "Кремнезем (SiO2)",
        description: "Оптимизирует сцепление на мокрой дороге и снижает сопротивление качению.",
        x: 111,
        y: 131,
        route: [
          { x: 111, y: 131 },
          { x: 199, y: 131 },
          { x: 199, y: 150 },
          { x: 286, y: 150 }
        ]
      },
      {
        id: "dssk",
        label: "Каучук DSSK",
        description: "Снижает расход топлива и поддерживает курсовую устойчивость на высоких скоростях.",
        x: 127,
        y: 67,
        route: [
          { x: 127, y: 67 },
          { x: 207, y: 67 },
          { x: 207, y: 150 },
          { x: 286, y: 150 }
        ]
      },
      {
        id: "carbonates",
        label: "Карбонатные добавки",
        description: "Повышают износостойкость протектора и продлевают рабочий ресурс шины.",
        x: 125,
        y: 213,
        route: [
          { x: 125, y: 213 },
          { x: 206, y: 213 },
          { x: 206, y: 150 },
          { x: 286, y: 150 }
        ]
      },
      {
        id: "polymers",
        label: "Силикон-содержащие полимеры",
        description: "Усиливают устойчивость на мокром покрытии и стабилизируют поведение шины.",
        x: 144,
        y: 261,
        route: [
          { x: 144, y: 261 },
          { x: 215, y: 261 },
          { x: 215, y: 150 },
          { x: 286, y: 150 }
        ]
      }
    ]
  };
  const CORDIANT_TECH_HOTSPOTS_BY_SLIDE = {
    "run-tour": {
      silica: {
        label: "ST-корд",
        description: "В Cordiant Run Tour применен корд сверхвысокой прочности ST (Super high Tensile), повышающий ударостойкость на неровных дорогах, и расширенные слои каркаса."
      },
      dssk: {
        label: "Нейлоновый бандаж",
        description: "Два слоя резины в боковине обеспечивают дополнительную защиту от боковых порезов и ударов при наезде на бордюры или выбоины. За защиту протекторной области от проколов отвечает усиленный нейлоновый бандаж."
      },
      carbonates: {
        label: "Дренажные канавки",
        description: "Симметрично направленный рисунок обеспечивает стабильность на прямой и предсказуемое поведение в поворотах. Диагональные канавки, расположенные под острым углом к направлению движения, уменьшают деформацию протектора и сокращают длину тормозного пути на мокрой дороге. Глубокие и широкие дренажные каналы эффективно отводят воду из пятна контакта, снижая риск аквапланирования."
      },
      polymers: {
        label: "Variable Pitch",
        description: "Благодаря асимметричному расположению элементов протектора (технология Variable Pitch Optimization) минимизируются воздушные завихрения, что снижает уровень шума даже на высокой скорости. Модель выпускается в диаметрах 13-17 дюймов, подходит для компактных городских авто и среднеразмерных седанов."
      }
    },
    gravity: {
      silica: {
        label: "DRY-COR",
        description: "Технология DRY-COR, примененная в Cordiant Gravity, улучшает характеристики на сухих дорогах. Жесткие шашечные элементы плечевых ребер не дают шине деформироваться в повороте, сохраняя стабильность пятна контакта и сцепления с дорогой, предотвращая сносы и заносы."
      },
      dssk: {
        label: "LONG-COR",
        description: "На двух центральных ребрах предусмотрены скосы, за счет которых увеличивается количество граней зацепления при движении на мягких грунтах и грунтовой дороге. Такое ноу-хау, как LONG-COR, работает на износоустойчивость резинового состава без ущерба остальным характеристикам. Долговечность повышена за счет увеличения глубины протектора на 12%."
      },
      carbonates: {
        label: "WET-COR",
        description: "Технология WET-COR, представленная широкими водоотводными канавками-водоотводами, отвечает за поведение на мокрой дороге."
      },
      polymers: {
        label: "Скосы + шум 69 дБ",
        description: "При этом разноразмерные шашки протектора расположены так, чтобы исключить звуковой резонанс. Уровень шума снижен до 69 дБ."
      }
    },
    "gravity-suv": {
      silica: {
        label: "STONE-COR",
        description: "Конструктивно модель Cordiant Gravity SUV во многом повторяет Cordiant Gravity. Эта шина разработана специально для кроссоверов и внедорожников и адаптирована для российских условий эксплуатации, включая городские дороги и проселочные пути. Ее отличительная особенность - технология STONE-COR. За счет скосов на двух центральных ребрах увеличено количество граней зацепления при движении на мягких грунтах и гравийных дорогах."
      },
      dssk: {
        label: "Усиленный каркас",
        description: "Усиленный силовой каркас адаптирован для работы в сложных условиях, позволяет выдерживать значительные механические нагрузки на разрыв и растяжение."
      },
      carbonates: {
        label: "WET-COR",
        description: "Центральные канавки протектора оборудованы специальными камневыталкивающими шипами для быстрого самоочищения протектора от камней."
      },
      polymers: {
        label: "Асимметричный протектор",
        description: "Асимметричный ненаправленный протектор обеспечивает эффективное сцепление с дорогой."
      }
    },
    "comfort-2": {
      silica: {
        label: "Wet-Cor",
        description: "Комфорт, хорошая управляемость и безопасность езды поддерживаются в Cordiant Comfort 2 несколькими фирменными ноу-хау. Система Wet-Cor подразумевает продуманный дренаж для борьбы с аквапланированием. Особые канавки и ребра разной формы эффективно отводят воду из пятна контакта. Существенную роль играет также новая резиновая смесь с большим содержанием силики, которая позволила повысить сцепные свойства шины на мокрой поверхности, одновременно снизив коэффициент сопротивления качению."
      },
      dssk: {
        label: "Flex-Cor",
        description: "Система Flex-Cor подразумевает эластичные боковины, усиливающие амортизацию и снижающие ударную нагрузку на подвеску."
      },
      carbonates: {
        label: "Dry-Cor",
        description: "Технология Dry-Cor включает в себя три сплошных ребра протектора для курсовой устойчивости на прямой. Устойчивость в поворотах и при перестроении обеспечивает внешняя сторона протектора, состоящая из крупных шашек, объединенных общим ребром жесткости."
      },
      polymers: {
        label: "12 элементов протектора",
        description: "На комфорт работает также инновационная методика чередования особых элементов протектора. Его асимметричный рисунок создан путем комбинирования 12 уникальных элементов - так на скорости снижается вероятность появления резонансных колебаний и дополнительных шумов."
      }
    }
  };
  const CORDIANT_ACCORDION_ITEMS = [
    {
      title: "Этапы производства",
      image: "assets/img/Этапы производства.jpg",
      paragraphs: [
        "Производство шин Cordiant включает несколько ключевых этапов. Все начинается с того, что инженеры научно-технического центра «Интайр» создают виртуальные версии будущих шин, включая дизайн протектора, боковин и другие параметры.",
        "Резиновые смеси готовятся в миксерах с точным дозированием компонентов: натурального и синтетического каучука, технического углерода (сажи), серы, кремнезема и других составляющих. Готовые смеси проходят контроль качества. Из резиновых смесей изготавливают отдельные детали: обрезиненный корд, каркас, брекер, боковины, протектор. Лента резины затем экструдируется в нужную форму с учетом дизайна протектора. На стадии сборки составляющие шины соединяются вместе.",
        "Затем в процессе вулканизации, который проходит при температуре почти 170°C и давлении около 16,5 бар, шина приобретает окончательную форму и физико-механические свойства. Каждая выпущенная шина проходит контроль качества, часть партии отправляют на рентген-установку. После успешного контроля качества шины отправляются на склад хранения готовой продукции."
      ]
    },
    {
      title: "Особенности производственных площадок",
      image: "assets/img/hero/Подиум Кордиант 2026/3 блок/Особенности производственных площадок.JPG",
      paragraphs: [
        "Производство легковых шин Cordiant на заводах в Омске, Ярославле, и Ульяновске имеет свои особенности, связанные с историей предприятий, технологиями, специализацией и производственными мощностями. Завод «Кордиант-Восток» специализируется на выпуске легковых и легкогрузовых шин, а также шин для внедорожников под брендами Cordiant и Tunga. На предприятии трудится более 850 квалифицированных специалистов.",
        "Заводы Gislaved в Калуге и Ульяновске вошли в структуру холдинга «Кордиант» в 2023 и 2024 годах соответственно, ранее эти заводы принадлежали шинным концернам Continental и Bridgestone. Здесь выпускают зимние и летние модели шин Cordiant (средний ценовой сегмент) и Gislaved (премиум сегмент) для легковых автомобилей, кроссоверов и внедорожников. Общая производственная мощность холдинга «Кордиант» - до 15 млн шин в год."
      ]
    },
    {
      title: "Квалифицированный персонал",
      image: "assets/img/hero/Подиум Кордиант 2026/3 блок/Квалифицированный персонал.JPG",
      paragraphs: [
        "В рамках холдинга «Кордиант» действует единый научно-технический центр разработки продукции, объединивший технологов, ранее работавших на зарубежные бренды. Холдинг сотрудничает с ведущими российскими университетами в рамках программ целевой подготовки кадров. Все это позволяет индустриализировать новые модели шин быстро и качественно.",
        "Еще один важный инструмент качесвтенной подготовки кадров - система передачи опыта и наставничества, являющаяся частью комплексной программы развития персонала. Также у каждого сотрудника компании есть возможность развивать свои личные и профессиональные навыки в рамках выстроенной системы обучения."
      ]
    },
    {
      title: "Качество и безопасность",
      image: "assets/img/hero/Подиум Кордиант 2026/3 блок/Качество и безопасность.JPG",
      paragraphs: [
        "Холдинг «Кордиант» уделяет большое внимание качеству и безопасности продукции. Компания применяет многоступенчатый процесс проверки. В 2024 году компания начала активно применять технологи компьютерного зрения. Это позволяет автоматически выявлять потенциальные дефекты в шинах и группировать их по различным критериям.",
        "По заявлению генерального директора АО «Кордиант» Вадима Володина, инновационные решения включают 100-процентную точность распознавания, автоматическую детекцию, энергоэффективность, полное сопровождение и безопасность труда.",
        "Предприятия холдинга выпускают продукцию высокого качества с бережным отношением к энергоресурсам, эффективным расходом материалов и соблюдая требования безопасности труда. В январе 2026 года эксперты Роскачества подтвердили экологическую безопасность шин Cordiant в ходе независимых испытаний."
      ]
    },
    {
      title: "Производственная линейка и стратегия развития",
      image: "assets/img/hero/Подиум Кордиант 2026/3 блок/Производственная линейка и стратегия развития.JPG",
      paragraphs: [
        "На четырех производственных площадках выпускаются шины Cordiant диаметром от 13 до 18 дюймов для легковых и легкогрузовых автомобилей. Стратегия холдинга «Кордиант» предусматривает в ближайшей перспективе выпуск наиболее востребованных типоразмеров для максимального покрытия парка легковых автомобилей."
      ]
    },
    {
      title: "Развитие технологий",
      image: "assets/img/hero/Подиум Кордиант 2026/3 блок/Развитие технологий.JPG",
      paragraphs: [
        "Бренд Cordiant активно развивает технологии в производстве шин, внедряя инновационные решения для повышения износостойкости, управляемости, безопасности и топливной экономичности. Инженеры НТЦ «Интайр» работают над созданием новых рецептур резиновых смесей, используя собственные наработки, а также технологии, унаследованные от бывших владельцев заводов (Continental и Bridgestone). Это включает разработку смесей с повышенной однородностью материала, что улучшает качество шин. Продолжается модернизация производственных мощностей, включая внедрение автоматизированных систем, расширяется сотрудничество с вузами и институтами."
      ]
    }
  ];
  const CORDIANT_ACCORDION_DEFAULT_INDEX = 0;
  const header = document.querySelector("[data-header]");
  const hero = document.querySelector(".hero--immersive");
  const drawer = document.querySelector("[data-drawer]");
  const toggle = document.querySelector("[data-menu-toggle]");
  const closeBtn = document.querySelector("[data-menu-close]");
  const setText = (k, v) => { document.querySelectorAll(`[data-content="${k}"]`).forEach((n) => { n.textContent = v; }); };
  const updHeader = () => {
    if (!header) return;
    header.classList.toggle("is-sticky", window.scrollY > 8);
    if (!hero || hero.hasAttribute("hidden")) {
      header.classList.remove("is-on-hero");
      return;
    }
    const b = hero.offsetTop + hero.offsetHeight - header.offsetHeight;
    header.classList.toggle("is-on-hero", window.scrollY < b);
  };
  const openDrawer = () => { if (!drawer) return; drawer.hidden = false; requestAnimationFrame(() => drawer.classList.add("is-open")); if (toggle) toggle.setAttribute("aria-expanded", "true"); };
  const closeDrawer = (now = false) => { if (!drawer) return; drawer.classList.remove("is-open"); if (toggle) toggle.setAttribute("aria-expanded", "false"); const fn = () => { if (!drawer.classList.contains("is-open")) drawer.hidden = true; }; now ? fn() : setTimeout(fn, 260); };
  const renderTimeline = () => {
    const root = document.getElementById("brand-timeline");
    if (!root) return;
    const milestones = [
      {
        year: "2006",
        title: "Первые модели",
        text: "Компания Cordiant прошла путь от предприятия под управлением нефтехимического холдинга до самостоятельного производителя. Первыми моделями бренда становятся летние шины Cordiant Sport и зимние нешипованные Cordiant Polar. Этот момент становится стартовым в формировании новой сильной корпорации по производству шин."
      },
      {
        year: "2009",
        title: "Центр «Интайр»",
        text: "В 2009 году создается собственный научно-исследовательский центр «Интайр» (г. Ярославль)."
      },
      {
        year: "2012",
        title: "Самостоятельная организация",
        text: "В 2012 году компания, ранее известная как «СИБУР – Русские шины», становится самостоятельной организацией под названием «Кордиант»."
      },
      {
        year: "2024",
        title: "Севергрупп",
        text: "В 2023 году 100% акций АО «Кордиант» приобретает группа S8 Capital, а затем в октябре 2024 года — «Севергрупп». Производство холдинга сосредоточено на предприятиях в Ярославле, Омске, Ульяновске и Калуге. Приоритетами в управлении новыми активами является расширение продуктовой линейки шин и автокомпонентов с фокусом на высокий уровень качества продукции, а также инвестиции в НИОКР. Компания ищет новые рынки сбыта, так как изначально большинство шинных предприятий были ориентированы и на экспорт. Рассматриваются ЮАР, Алжир, Египет и некоторые другие страны."
      }
    ];
    const portfolioNote = "";
    root.innerHTML = milestones.map((item, idx) => {
      const isLast = idx === milestones.length - 1;
      const lineClass = idx === milestones.length - 1 ? " timeline-marker--end" : "";
      const extraNote = isLast && portfolioNote ? `<p class="timeline-note">${portfolioNote}</p>` : "";
      return `<li class="timeline-item" data-reveal style="--timeline-delay:${idx * 90}ms">
        <div class="timeline-year">${item.year}</div>
        <div class="timeline-marker${lineClass}" aria-hidden="true"><span></span></div>
        <article class="timeline-content">
          <h4 class="timeline-title">${item.title}</h4>
          <p class="timeline-text">${item.text}</p>
          ${extraNote}
        </article>
      </li>`;
    }).join("");
  };
  const renderBrandStoryPanels = () => {
    const tabs = {
      brand: {
        title: "О бренде",
        text: C.brand.intro
      },
      symbolism: {
        title: "Символика бренда",
        text: C.brand.symbolism
      },
      testing: {
        title: "Испытательная база",
        text: C.brand.testing,
        points: ["ПИЦ «Вершина»", "Полигон НАМИ", "Полигон СВФУ, ТОР «Якутия»", "DEKRA / IDIADA / DUK Test"]
      },
      rnd: {
        title: "Исследовательский центр",
        text: C.brand.rnd
      },
      lab: {
        title: "Лабораторный контроль",
        text: C.brand.lab
      },
      advantages: {
        title: "Преимущества",
        text: C.brand.advantages
      }
    };
    Object.entries(tabs).forEach(([key, data]) => {
      const panel = document.querySelector(`.milestones-panel[data-panel="${key}"]`);
      if (!panel) return;
      if (key === "brand") {
        panel.innerHTML = `<article class="milestones-brand">
          <span class="milestones-brand__tag">О бренде</span>
          <img class="milestones-brand__logo" src="assets/icons/logo-brand.svg" alt="Cordiant" width="1125" height="114" loading="lazy" />
          <p class="milestones-brand__lead">Cordiant - это ведущий российский бренд шин в составе продуктового портфеля холдинга АО «Кордиант», одного из крупнейших производителей шинной продукции.</p>
          <div class="milestones-brand__text">
            <p class="milestones-brand__p">
              Производство шин под брендом Cordiant началось в 2006 году на базе Ярославского и Омского шинных заводов. Сегодня под брендом Cordiant производятся шины для легковых и грузовых автомобилей, а также для легких грузовиков.
            </p>
            <p class="milestones-brand__p">
              Для этого задействованы заводы в Омске, Ярославле, Калуге и Ульяновске. Продукция бренда реализуется через сеть дистрибьюторов, насчитывающую более 300 партнеров по всей России.
            </p>
          </div>
          <div class="milestones-brand__stats" aria-label="Ключевые факты о бренде Cordiant">
            <article class="milestones-brand__stat"><strong>400</strong><span>моделей</span></article>
            <article class="milestones-brand__stat"><strong>300</strong><span>партнеров</span></article>
            <article class="milestones-brand__stat"><strong>с 2006</strong><span>производство</span></article>
          </div>
        </article>`;
        return;
      }
      if (key === "symbolism") {
        panel.innerHTML = `<article class="milestones-symbol">
          <div class="milestones-symbol__text">
            <h4>Символика бренда</h4>
            <p class="milestones-symbol__p milestones-symbol__p--lead">
              Логотип Cordiant изначально включал название бренда и след от лапки геккона – ящерицы, которая умеет перемещаться по стенам и потолкам. Этот образ символизирует цепкость производимых шин.
            </p>
            <p class="milestones-symbol__p milestones-text-secondary">
              Полоса, идущая по спине ящерицы через весь логотип, добавляет динамики. Кроме геккона, Cordiant использует в рекламе образы акулы и барса, что символизирует естественное сцепление шин с мокрыми и заснеженными дорогами.
            </p>
            <p class="milestones-symbol__p">
              В 2023 году в рамках продвижения бренда и улучшения визуализации и узнаваемости логотип был модифицирован. Символизировавшая бренд лапка геккона была заменена стилизованным изображением самой ящерицы. Акцент с лапки на фигурку геккона сделало логотип более ярким и запоминающимся.
            </p>
          </div>
          <figure class="milestones-symbol__media">
            <img src="assets/img/freepik__-cordiant__83852.png" alt="Символика бренда Cordiant" width="1024" height="1024" loading="lazy" />
          </figure>
        </article>`;
        return;
      }
      if (key === "testing") {
        panel.innerHTML = `<article class="milestones-testing">
          <div class="milestones-testing__text">
            <h4>Испытательная база</h4>
            <p class="milestones-testing__p milestones-testing__p--lead">
              Вся новая продукция бренда проходит многоэтапные испытания. Сначала проводятся стендовые тесты в лаборатории, а затем шины отправляются на полигон для дорожных испытаний.
            </p>
            <p class="milestones-testing__p">
              Испытания проводятся на полигонах в различных географических широтах: в частности, в Московской и Ярославской областях, а также в Якутии.
            </p>
            <div class="milestones-testing__chips" aria-label="Собственные полигоны Cordiant">
              <span class="milestones-testing__chip">Московская область</span>
              <span class="milestones-testing__chip">Ярославская область</span>
              <span class="milestones-testing__chip">Якутия</span>
            </div>
            <p class="milestones-testing__p">
              Работа в лабораториях, как и тесты шинных новинок на различных тестовых площадках, позволяют убедиться в высококонкурентных качествах продукции Cordiant.
            </p>
          </div>
          <figure class="milestones-testing__media">
            <img src="assets/img/hero/Подиум Кордиант 2026/1 блок/Испытательная база.jpg" alt="Испытательная база Cordiant" width="1024" height="1024" loading="lazy" />
          </figure>
        </article>`;
        return;
      }
      if (key === "rnd") {
        panel.innerHTML = `<article class="milestones-testing">
          <div class="milestones-testing__text">
            <h4>Научно-исследовательский центр</h4>
            <p class="milestones-testing__p milestones-testing__p--lead">
              НТЦ «Интайр» – это уникальное научное подразделение холдинга «Кордиант».
            </p>
            <p class="milestones-testing__p">
              Центральный офис находится в Москве, а на производственных площадках в Калуге, Ульяновске, Омске и Ярославле действуют подразделения НТЦ.
            </p>
            <p class="milestones-testing__p">
              Компетенция НТЦ включает компьютерное моделирование шин, разработку дизайна и конструкции, создание новых резиновых смесей, омологацию нового сырья и материалов, а также инжиниринг технологических процессов производства. Центр сотрудничает с ведущими мировыми институтами и испытательными полигонами, включая полигон «Вершина» в Ярославле, где тестируются все типы шин.
            </p>
            <p class="milestones-testing__p">
              Разрабатываемая продукция проходит дорожные, стендовые и сертификационные испытания. Наличие собственного R&D-центра с высокой экспертизой позволяет следовать технологическим трендам и эффективно расширять ассортимент выпускаемой продукции.
            </p>
          </div>
          <figure class="milestones-testing__media">
            <img src="assets/img/hero/Подиум Кордиант 2026/1 блок/Исследовательский центр.JPG" alt="Исследовательский центр НТЦ Интайр" width="1024" height="1024" loading="lazy" />
          </figure>
        </article>`;
        return;
      }
      if (key === "lab") {
        panel.innerHTML = `<article class="milestones-testing">
          <div class="milestones-testing__text">
            <h4>Лабораторный контроль и система качества</h4>
            <p class="milestones-testing__p milestones-testing__p--lead">
              Массовое производство шин требует высокого качества поступающего сырья и материалов.
            </p>
            <p class="milestones-testing__p">
              В холдинге «Кордиант» каждая единица сырья проходит множество тестов в лабораториях, которые действуют на всех заводах холдинга.
            </p>
            <p class="milestones-testing__p">
              Здесь проводятся масштабные испытания, включая тестирование компаунда, элементов каркаса и внутренней структуры. Также проводятся исследования свойств новых рецептур, испытания новых режимов смешения и вулканизации, а также приготовление шинных срезов. Лаборатории разрабатывают собственные резиновые смеси и проводят физико-механические испытания и различные тесты, чтобы убедиться, что они соответствуют высоким требованиям качества, безопасности и инновационности.
            </p>
            <div class="milestones-testing__standards" aria-label="Стандарты лабораторного контроля">
              <img class="milestones-testing__standard-icon" src="assets/icons/ASTM_logo 1.svg" alt="ASTM International" width="160" height="90" loading="lazy" />
              <img class="milestones-testing__standard-icon" src="assets/icons/ISO_Logo_(Red_square) 1.svg" alt="ISO" width="120" height="90" loading="lazy" />
              <img class="milestones-testing__standard-icon" src="assets/icons/gost.svg" alt="ГОСТ" width="170" height="90" loading="lazy" />
            </div>
          </div>
          <figure class="milestones-testing__media">
            <img src="assets/img/hero/Подиум Кордиант 2026/1 блок/Лабораторный контроль и система качества.JPG" alt="Лабораторный контроль и система качества Cordiant" width="1024" height="1024" loading="lazy" />
          </figure>
        </article>`;
        return;
      }
      if (key === "advantages") {
        panel.innerHTML = `<article class="milestones-advantages">
          <div class="milestones-advantages__head">
            <h4>Конкурентные преимущества</h4>
            <p class="milestones-advantages__lead">
              Продукция бренда пользуется стабильно высоким спросом, прежде всего, благодаря передовым технологиям производства, благодаря которым шины Cordiant не уступают моделям-конкурентам премиум-сегмента
            </p>
            <p class="milestones-advantages__summary">
              Слагаемые высокой ходимости - инновационные смеси и оптимизированная структура рисунка протектора. На амортизирующие возможности работает облегченный материал корда и эластичные боковины. В то же время продукция бренда отличается ценовой доступностью. Не уступая по уровню комфорта и безопасности известным западным брендам, шины Cordiant оказываются достойной альтернативой премиум-сегменту.
            </p>
          </div>
          <div class="milestones-advantages__grid" aria-label="Ключевые конкурентные преимущества шин Cordiant">
            <article class="milestones-advantages__item">
              <span class="milestones-advantages__icon-shell">
                <img class="milestones-advantages__icon" src="assets/icons/1.svg" alt="Стабильный спрос" width="50" height="50" loading="lazy" />
              </span>
              <p class="milestones-advantages__item-text">
                На амортизирующие возможности работает облегченный материал корда и эластичные боковины.
              </p>
            </article>
            <article class="milestones-advantages__item milestones-advantages__item--accent">
              <span class="milestones-advantages__icon-shell">
                <img class="milestones-advantages__icon" src="assets/icons/2.svg" alt="Экологические характеристики" width="50" height="50" loading="lazy" />
              </span>
              <p class="milestones-advantages__item-text">
                Устойчивость к аквапланированию, курсовая устойчивость и эффективность торможения достигаются за счет компьютерного моделирования протектора в собственном НТЦ «Интайр».
              </p>
            </article>
            <article class="milestones-advantages__item">
              <span class="milestones-advantages__icon-shell">
                <img class="milestones-advantages__icon" src="assets/icons/3.svg" alt="Устойчивость и торможение" width="50" height="50" loading="lazy" />
              </span>
              <p class="milestones-advantages__item-text">
                Надежность конструкции обеспечивается современными технологиями укрепления каркаса и инновационной системой укладки слоев компаунда.
              </p>
            </article>
            <article class="milestones-advantages__item">
              <span class="milestones-advantages__icon-shell">
                <img class="milestones-advantages__icon" src="assets/icons/4.svg" alt="Надежность конструкции" width="50" height="50" loading="lazy" />
              </span>
              <p class="milestones-advantages__item-text">
                Слагаемые высокой ходимости - инновационные смеси и оптимизированная структура рисунка протектора.
              </p>
            </article>
          </div>
        </article>`;
        return;
      }
      const points = Array.isArray(data.points) && data.points.length
        ? `<ul class="milestones-panel__list">${data.points.map((point) => `<li>${point}</li>`).join("")}</ul>`
        : "";
      panel.innerHTML = `<article class="milestones-panel__content">
        <h4>${data.title}</h4>
        <p>${data.text}</p>
        ${points}
      </article>`;
    });
  };
  const renderModels = () => {
    const root = document.getElementById("all-models-grid");
    if (!root) return;
    root.innerHTML = C.allModels.map((m) => {
      const photo = modelPhotoMap[m.key];
      const isFeatured = Boolean(photo);
      const mediaClass = `model-card__media${isFeatured ? " model-card__media--photo" : ""}`;
      const cardClass = `model-card${isFeatured ? " model-card--featured" : ""}`;
      const src = photo || m.image;
      const width = isFeatured ? 1024 : 320;
      const height = isFeatured ? 1024 : 220;
      return `<article class="${cardClass}" data-model="${m.key}">
        <figure class="${mediaClass}">
          <img src="assets/img/hero-tire.svg" data-src="${src}" alt="Шина Cordiant ${m.name}" width="${width}" height="${height}" loading="lazy" />
        </figure>
        <h3>${m.name}</h3>
        <p class="model-card__lead">${modelLeads[m.key] || "Модель линейки Cordiant"}</p>
        <div class="model-tags">${m.tags.map((t) => `<span>${t}</span>`).join("")}</div>
      </article>`;
    }).join("");
  };
  const renderPanels = (id, grp, data) => {
    const root = document.getElementById(id);
    if (!root) return;
    const isSummer = grp === "summer-feature";
    const isModelTech = grp === "model-tech";
    root.classList.add("tab-panels");
    root.innerHTML = Object.entries(data).map(([k, v], i) => {
      const meta = isSummer ? summerFeatureMeta[k] || {} : isModelTech ? modelTechMeta[k] || {} : {};
      const summary = v.summary || v.text || "";
      const scenario = isSummer
        ? `<p class="tab-panel__scenario">${meta.scenario || "Сценарий эксплуатации уточняется"}</p>`
        : `<p class="tab-panel__scenario">Технологический профиль модели ${v.title}</p>`;
      const benefits = isSummer && Array.isArray(meta.benefits)
        ? `<div class="tab-panel__benefits"><ul>${meta.benefits.map((b) => `<li>${b}</li>`).join("")}</ul></div>`
        : "";
      const techList = (meta.tech || v.tags || []).filter(Boolean);
      const tech = techList.length
        ? `<div class="tab-panel__tech"><ul>${techList.map((t) => `<li>${t}</li>`).join("")}</ul></div>`
        : "";
      const image = meta.image || v.image || "assets/img/hero-tire.svg";
      const useTireShell = !meta.image && !isSummer;
      const imageShell = `tab-panel__image-shell${useTireShell ? " tab-panel__image-shell--tire" : ""}`;
      const active = i === 0;
      return `<section class="tab-panel${active ? " is-active" : ""}" data-panel-group="${grp}" data-panel="${k}" data-model="${k}" role="tabpanel"${active ? "" : " hidden"}>
        <div class="tab-panel__body">
          <div class="tab-panel__head">
            <h4>${v.title}</h4>
            <div class="tab-panel__meta">${(v.tags || []).map((t) => `<span>${t}</span>`).join("")}</div>
          </div>
          <p class="tab-panel__summary">${summary}</p>
          ${scenario}
          ${benefits}
          ${tech}
        </div>
        <figure class="${imageShell}">
          <img class="tab-panel__image" src="assets/img/hero-tire.svg" data-src="${image}" alt="${v.title}" width="1024" height="1024" loading="lazy" />
        </figure>
      </section>`;
    }).join("");
  };
  const renderLogic = () => { const root = document.getElementById("russia-logic-grid"); if (!root) return; root.innerHTML = C.russia.logic.map((i) => `<article class="logic-card" data-reveal><h4>${i.condition}</h4><p><span class="logic-arrow">Решение:</span> ${i.solution}</p><p><span class="logic-arrow">Выгода:</span> ${i.benefit}</p></article>`).join(""); };
  const renderMaterials = () => {
    const root = document.getElementById("materials-components");
    if (!root) return;
    root.innerHTML = C.materials.components.map((i, n) => `<article class="component-card" data-material-card="${n + 1}">
      <h4>${i.title}</h4>
      <p><strong>Что это:</strong> ${i.what}</p>
      <p><strong>Что дает:</strong> ${i.effect}</p>
    </article>`).join("");
  };
  const renderReasons = () => {
    const root = document.getElementById("reasons-grid");
    if (!root) return;
    root.innerHTML = C.reasons.map((item) => {
      const hasVideo = typeof item.video === "string" && item.video.trim().length > 0;
      const variant = hasVideo ? " reason-card--video" : (item.variant === "solid" ? " reason-card--solid" : " reason-card--photo");
      const style = item.image ? ` style="--reason-image:url('${item.image}')"` : "";
      const media = hasVideo
        ? `<video class="reason-card__video" autoplay muted loop playsinline preload="metadata" aria-hidden="true">
             <source src="${item.video}" type="video/mp4" />
           </video>`
        : "";
      return `<article class="reason-card${variant}" data-reveal${style}>
        ${media}
        <div class="reason-card__body">
          <h3>${item.title}</h3>
          <p>${item.text}</p>
        </div>
      </article>`;
    }).join("");
  };
  const splitSummerText = (text) => {
    const raw = String(text || "").replace(/\r/g, "").trim();
    if (!raw) return { lead: "", rest: "" };
    const paragraphs = raw
      .split(/\n{2,}/)
      .map((paragraph) => paragraph.replace(/\n+/g, " ").replace(/\s+/g, " ").trim())
      .filter(Boolean);
    if (paragraphs.length > 1) {
      return {
        lead: paragraphs[0],
        rest: paragraphs.slice(1).join("\n\n")
      };
    }
    const clean = (paragraphs[0] || raw).replace(/\s+/g, " ").trim();
    if (!clean) return { lead: "", rest: "" };
    const sentencePattern = /[^.!?]+[.!?]+(?:\s+|$)/g;
    const parts = [];
    let match;
    while ((match = sentencePattern.exec(clean)) !== null) {
      const sentence = match[0].trim();
      if (sentence) parts.push(sentence);
    }
    if (!parts.length) return { lead: clean, rest: "" };
    const lead = parts[0];
    const rest = clean.slice(lead.length).trim();
    return { lead, rest };
  };
  const buildSummerModels = () => {
    const featured = C.summerFeatured || {};
    return (C.allModels || []).filter((model) => {
      const summary = featured[model.key]?.summary;
      return typeof summary === "string" && summary.trim().length > 0;
    }).map((model) => {
      const featuredData = featured[model.key] || {};
      const sourceText = featuredData.summary;
      const split = splitSummerText(sourceText);
      const rawTitle = featuredData?.title || model.name;
      const title = rawTitle.replace(/^Cordiant\s+/i, "");
      const chips = (Array.isArray(featuredData?.tags) && featuredData.tags.length ? featuredData.tags : model.tags || []).slice(0, 4);
      return {
        key: model.key,
        name: model.name,
        title,
        image: featuredData?.image || model.image || "assets/img/hero-tire.svg",
        photo: summerCardPhotoMap[model.key] || summerCardPhotoFallback,
        lead: split.lead || sourceText,
        description: split.rest,
        chips
      };
    });
  };
  const renderSummerPodium = () => {
    const modelsHost = document.querySelector("[data-summer-models]");
    const title = document.querySelector("[data-summer-title]");
    const lead = document.querySelector("[data-summer-lead]");
    const image = document.querySelector("[data-summer-image]");
    const photo = document.querySelector("[data-summer-photo]");
    const chips = document.querySelector("[data-summer-chips]");
    const desc = document.querySelector("[data-summer-desc]");
    const more = document.querySelector("[data-summer-more]");
    const expanded = document.querySelector("[data-summer-expanded]");
    const expandedColumns = document.querySelector("[data-summer-expanded-columns]");
    const prev = document.querySelector("[data-summer-prev]");
    const next = document.querySelector("[data-summer-next]");
    const dotsHost = document.querySelector("[data-summer-dots]");
    if (!(modelsHost instanceof HTMLElement)
      || !(title instanceof HTMLElement)
      || !(lead instanceof HTMLElement)
      || !(image instanceof HTMLImageElement)
      || !(photo instanceof HTMLImageElement)
      || !(chips instanceof HTMLElement)
      || !(desc instanceof HTMLElement)
      || !(more instanceof HTMLButtonElement)
      || !(expanded instanceof HTMLElement)
      || !(expandedColumns instanceof HTMLElement)
      || !(prev instanceof HTMLButtonElement)
      || !(next instanceof HTMLButtonElement)
      || !(dotsHost instanceof HTMLElement)) return;
    const models = buildSummerModels();
    if (!models.length) return;
    const interactiveKeys = new Set(models.map((model) => model.key));
    const allModelsForIntro = Array.isArray(C.allModels) && C.allModels.length ? C.allModels : models;
    modelsHost.innerHTML = allModelsForIntro.map((model, index) => {
      const separator = index < allModelsForIntro.length - 1 ? '<span class="summer-podium__model-sep">, </span>' : "";
      if (interactiveKeys.has(model.key)) {
        return `<button class="summer-podium__model-btn" type="button" data-summer-model="${model.key}">${model.name}</button>${separator}`;
      }
      return `<span class="summer-podium__model-name">${model.name}</span>${separator}`;
    }).join("");
    dotsHost.innerHTML = models.map((model, index) => (
      `<button class="summer-podium__dot" type="button" data-summer-dot="${index}" aria-label="Показать модель ${model.name}"></button>`
    )).join("");
    let activeIndex = Math.max(0, models.findIndex((item) => item.key === "runTour"));
    const splitExtraTextIntoColumns = (text) => {
      const clean = String(text || "").replace(/\r/g, "").trim();
      if (!clean) return ["", ""];
      const paragraphs = clean
        .split(/\n{2,}/)
        .map((paragraph) => paragraph.replace(/\n+/g, " ").replace(/\s+/g, " ").trim())
        .filter(Boolean);
      if (paragraphs.length > 1) {
        const half = Math.ceil(paragraphs.length / 2);
        return [
          paragraphs.slice(0, half).join("\n\n"),
          paragraphs.slice(half).join("\n\n")
        ];
      }
      const sentences = clean
        .match(/[^.!?]+[.!?]+|[^.!?]+$/g)
        ?.map((sentence) => sentence.trim())
        .filter(Boolean) || [clean];
      if (sentences.length === 1) return [sentences[0], ""];
      const targetLength = clean.length / 2;
      let currentLength = 0;
      let splitIndex = 1;
      for (let i = 0; i < sentences.length; i += 1) {
        currentLength += sentences[i].length + (i > 0 ? 1 : 0);
        if (currentLength >= targetLength) {
          splitIndex = i + 1;
          break;
        }
      }
      return [
        sentences.slice(0, splitIndex).join(" ").trim(),
        sentences.slice(splitIndex).join(" ").trim()
      ];
    };
    let isExpanded = false;
    const hideExpanded = () => {
      expanded.hidden = true;
      expandedColumns.innerHTML = "";
      more.setAttribute("aria-expanded", "false");
    };
    const showExpanded = (text) => {
      const [leftText, rightText] = splitExtraTextIntoColumns(text);
      const createColumn = (content) => {
        const node = document.createElement("p");
        node.className = "summer-podium__expanded-col";
        node.textContent = content;
        return node;
      };
      expandedColumns.innerHTML = "";
      expandedColumns.appendChild(createColumn(leftText));
      expandedColumns.appendChild(createColumn(rightText || ""));
      expanded.hidden = false;
      more.setAttribute("aria-expanded", "true");
    };
    const syncExpanded = (model) => {
      const extraText = (model?.description || "").trim();
      more.hidden = !extraText;
      if (!extraText || !isExpanded) {
        hideExpanded();
        return;
      }
      showExpanded(extraText);
    };
    const apply = (index) => {
      const total = models.length;
      if (!total) return;
      activeIndex = ((index % total) + total) % total;
      const model = models[activeIndex];
      title.textContent = model.title;
      lead.textContent = model.lead;
      image.src = model.image;
      image.alt = `Шина Cordiant ${model.name}`;
      photo.src = model.photo;
      photo.alt = `Фотография для модели ${model.name}`;
      chips.innerHTML = model.chips.map((chip) => `<span>${chip}</span>`).join("");
      desc.hidden = !(model.description || "").trim();
      syncExpanded(model);
      modelsHost.querySelectorAll("[data-summer-model]").forEach((button) => {
        const isActive = button.getAttribute("data-summer-model") === model.key;
        button.classList.toggle("is-active", isActive);
      });
      dotsHost.querySelectorAll("[data-summer-dot]").forEach((button, dotIndex) => {
        const isActive = dotIndex === activeIndex;
        button.classList.toggle("is-active", isActive);
        button.setAttribute("aria-current", isActive ? "true" : "false");
      });
    };
    modelsHost.addEventListener("click", (event) => {
      const button = event.target.closest("[data-summer-model]");
      if (!(button instanceof HTMLButtonElement)) return;
      const key = button.getAttribute("data-summer-model");
      const index = models.findIndex((model) => model.key === key);
      if (index >= 0) apply(index);
    });
    dotsHost.addEventListener("click", (event) => {
      const button = event.target.closest("[data-summer-dot]");
      if (!(button instanceof HTMLButtonElement)) return;
      const index = Number(button.getAttribute("data-summer-dot"));
      if (Number.isFinite(index)) apply(index);
    });
    more.addEventListener("click", () => {
      if (more.hidden) return;
      isExpanded = !isExpanded;
      syncExpanded(models[activeIndex]);
    });
    prev.addEventListener("click", () => apply(activeIndex - 1));
    next.addEventListener("click", () => apply(activeIndex + 1));
    apply(activeIndex);
  };
  const setupTabs = () => {
    document.querySelectorAll("[data-tab-group]").forEach((g) => {
      const name = g.getAttribute("data-tab-group");
      if (!name) return;
      const btns = Array.from(g.querySelectorAll("[data-tab]"));
      const pans = Array.from(document.querySelectorAll(`[data-panel-group="${name}"]`));
      if (!btns.length || !pans.length) return;
      btns.forEach((b) => {
        const key = b.getAttribute("data-tab");
        if (!key) return;
        b.id = `${name}-tab-${key}`;
      });
      pans.forEach((p) => {
        const key = p.getAttribute("data-panel");
        if (!key) return;
        p.id = `${name}-panel-${key}`;
        p.setAttribute("aria-labelledby", `${name}-tab-${key}`);
      });
      const act = (k) => {
        btns.forEach((b) => {
          const active = b.getAttribute("data-tab") === k;
          b.classList.toggle("is-active", active);
          b.setAttribute("aria-selected", active ? "true" : "false");
          b.setAttribute("tabindex", active ? "0" : "-1");
        });
        pans.forEach((p) => {
          const active = p.getAttribute("data-panel") === k;
          p.classList.toggle("is-active", active);
          p.hidden = !active;
          p.setAttribute("aria-hidden", active ? "false" : "true");
        });
      };
      const initial = btns.find((b) => b.classList.contains("is-active"))?.getAttribute("data-tab") || btns[0].getAttribute("data-tab");
      if (initial) act(initial);
      g.addEventListener("click", (e) => {
        const b = e.target.closest("[data-tab]");
        if (!b) return;
        const key = b.getAttribute("data-tab");
        if (key) act(key);
      });
      g.addEventListener("keydown", (e) => {
        const currentIndex = btns.findIndex((b) => b.classList.contains("is-active"));
        if (currentIndex < 0) return;
        let nextIndex = currentIndex;
        if (e.key === "ArrowRight" || e.key === "ArrowDown") nextIndex = (currentIndex + 1) % btns.length;
        else if (e.key === "ArrowLeft" || e.key === "ArrowUp") nextIndex = (currentIndex - 1 + btns.length) % btns.length;
        else return;
        e.preventDefault();
        const btn = btns[nextIndex];
        const key = btn.getAttribute("data-tab");
        if (!key) return;
        act(key);
        btn.focus();
      });
    });
  };
  const setupMilestonesMore = () => {
    const triggers = Array.from(document.querySelectorAll("[data-milestones-more-trigger]"));
    if (!triggers.length) return;
    const hideTimers = new WeakMap();
    triggers.forEach((button) => {
      if (!(button instanceof HTMLButtonElement)) return;
      button.addEventListener("click", () => {
        const expandSelector = button.getAttribute("data-milestones-expand-target");
        const expandTarget = expandSelector ? document.querySelector(expandSelector) : null;
        const isExpanded = button.getAttribute("aria-expanded") === "true";
        const nextExpanded = !isExpanded;
        button.setAttribute("aria-expanded", nextExpanded ? "true" : "false");
        if (expandSelector) {
          if (expandTarget instanceof HTMLElement) {
            expandTarget.classList.toggle("is-expanded", nextExpanded);
          }
        }
        const extraId = button.getAttribute("aria-controls");
        if (!extraId) return;
        const extra = document.getElementById(extraId);
        if (!(extra instanceof HTMLElement)) return;
        const pendingTimer = hideTimers.get(extra);
        if (pendingTimer) {
          window.clearTimeout(pendingTimer);
          hideTimers.delete(extra);
        }
        if (nextExpanded) {
          extra.hidden = false;
          if (!rdm()) {
            requestAnimationFrame(() => extra.classList.add("is-open"));
          } else {
            extra.classList.add("is-open");
          }
          extra.setAttribute("aria-hidden", "false");
          return;
        }
        extra.classList.remove("is-open");
        extra.setAttribute("aria-hidden", "true");
        if (rdm()) {
          extra.hidden = true;
          return;
        }
        const timerId = window.setTimeout(() => {
          if (button.getAttribute("aria-expanded") === "false") {
            extra.hidden = true;
          }
          hideTimers.delete(extra);
        }, 360);
        hideTimers.set(extra, timerId);
      });
    });
  };
  const setupBrandStoryWheelTabs = () => {
    const section = document.querySelector(".milestones-screen");
    const group = document.querySelector('[data-tab-group="brand-story"]');
    const panelsWrap = section?.querySelector(".milestones-panels");
    if (!section || !group || !panelsWrap) return;
    const buttons = () => Array.from(group.querySelectorAll("[data-tab]"));
    if (!buttons().length) return;
    let lastSwitchAt = 0;
    const canHandle = () => {
      const rect = section.getBoundingClientRect();
      const topLimit = Math.max(0, (header?.offsetHeight || 0) + 6);
      return rect.top <= topLimit && rect.bottom > window.innerHeight * 0.55;
    };
    const activeIndex = () => buttons().findIndex((btn) => btn.classList.contains("is-active"));
    const switchTo = (index) => {
      const btns = buttons();
      const target = btns[index];
      if (!target) return false;
      target.click();
      panelsWrap.scrollTop = 0;
      return true;
    };
    const canScrollInside = (deltaY) => {
      const maxScroll = panelsWrap.scrollHeight - panelsWrap.clientHeight;
      if (maxScroll <= 1) return false;
      if (deltaY > 0) return panelsWrap.scrollTop < maxScroll - 1;
      if (deltaY < 0) return panelsWrap.scrollTop > 1;
      return false;
    };
    window.addEventListener("wheel", (event) => {
      if (!canHandle()) return;
      if (Math.abs(event.deltaY) < 10) return;
      if (canScrollInside(event.deltaY)) return;
      const now = Date.now();
      if (now - lastSwitchAt < 380) {
        event.preventDefault();
        return;
      }
      const idx = activeIndex();
      if (idx < 0) return;
      if (event.deltaY > 0) {
        const next = idx + 1;
        if (next < buttons().length) {
          if (switchTo(next)) {
            lastSwitchAt = now;
            event.preventDefault();
          }
        }
      } else if (event.deltaY < 0) {
        const prev = idx - 1;
        if (prev >= 0) {
          if (switchTo(prev)) {
            lastSwitchAt = now;
            event.preventDefault();
          }
        }
      }
    }, { passive: false });
  };
  const setupPresentationScroll = () => {
    const sections = Array.from(document.querySelectorAll("main#main-content > section.section"));
    if (sections.length < 2) return;
    let isLocked = false;
    let lockTimer = 0;
    let lockedTargetTop = null;
    const edgeThreshold = 18;
    const getSectionTop = (section) => Math.round(section.getBoundingClientRect().top + window.scrollY);
    const getSectionBottom = (section) => getSectionTop(section) + section.offsetHeight;
    const releaseLock = () => {
      isLocked = false;
      lockedTargetTop = null;
      if (lockTimer) {
        window.clearTimeout(lockTimer);
        lockTimer = 0;
      }
    };
    const lockFor = (duration, targetTop) => {
      isLocked = true;
      lockedTargetTop = targetTop;
      if (lockTimer) window.clearTimeout(lockTimer);
      lockTimer = window.setTimeout(releaseLock, duration);
    };
    const getCurrentIndex = () => {
      const anchorTop = window.scrollY + 2;
      let currentIndex = 0;
      sections.forEach((section, index) => {
        if (getSectionTop(section) <= anchorTop) currentIndex = index;
      });
      return currentIndex;
    };
    const canMoveFromSection = (sectionIndex, direction) => {
      const section = sections[sectionIndex];
      if (!section) return false;
      const sectionTop = getSectionTop(section);
      const sectionBottom = getSectionBottom(section);
      if (direction > 0) {
        const viewportBottom = window.scrollY + window.innerHeight;
        return viewportBottom >= sectionBottom - edgeThreshold;
      }
      return window.scrollY <= sectionTop + edgeThreshold;
    };
    const moveToIndex = (nextIndex) => {
      const clamped = Math.max(0, Math.min(sections.length - 1, nextIndex));
      const target = sections[clamped];
      if (!target) return;
      const targetTop = getSectionTop(target);
      lockFor(rdm() ? 180 : 760, targetTop);
      window.scrollTo({
        top: targetTop,
        behavior: rdm() ? "auto" : "smooth"
      });
    };
    const canScrollInside = (target, deltaY) => {
      if (!(target instanceof Element)) return false;
      let node = target;
      while (node && node !== document.body) {
        const style = window.getComputedStyle(node);
        const overflowY = style.overflowY;
        const hasScroll = (overflowY === "auto" || overflowY === "scroll" || overflowY === "overlay")
          && node.scrollHeight > node.clientHeight + 1;
        if (hasScroll) {
          const canDown = deltaY > 0 && node.scrollTop + node.clientHeight < node.scrollHeight - 1;
          const canUp = deltaY < 0 && node.scrollTop > 1;
          if (canDown || canUp) return true;
        }
        node = node.parentElement;
      }
      return false;
    };
    window.addEventListener("wheel", (event) => {
      if (event.defaultPrevented) return;
      if (event.ctrlKey || Math.abs(event.deltaY) < 10) return;
      if (canScrollInside(event.target, event.deltaY)) return;
      const direction = event.deltaY > 0 ? 1 : -1;
      const current = getCurrentIndex();
      const next = current + direction;
      if (next < 0 || next >= sections.length) return;
      if (!canMoveFromSection(current, direction)) return;
      event.preventDefault();
      if (isLocked) return;
      moveToIndex(next);
    }, { passive: false });
    window.addEventListener("keydown", (event) => {
      if (event.defaultPrevented) return;
      const target = event.target;
      if (target instanceof HTMLElement) {
        const isTypingTarget = target.isContentEditable
          || ["INPUT", "TEXTAREA", "SELECT"].includes(target.tagName);
        if (isTypingTarget) return;
      }
      let direction = 0;
      if (event.key === "PageDown" || event.key === "ArrowDown") direction = 1;
      if (event.key === "PageUp" || event.key === "ArrowUp") direction = -1;
      if (event.key === " " && !event.shiftKey) direction = 1;
      if (event.key === " " && event.shiftKey) direction = -1;
      if (!direction) return;
      const current = getCurrentIndex();
      const next = current + direction;
      if (next < 0 || next >= sections.length) return;
      event.preventDefault();
      if (isLocked) return;
      moveToIndex(next);
    });
    window.addEventListener("scroll", () => {
      if (!isLocked || lockedTargetTop === null) return;
      if (Math.abs(window.scrollY - lockedTargetTop) < 3) releaseLock();
    }, { passive: true });
  };
  const setupAnchor = () => { document.addEventListener("click", (e) => { const a = e.target.closest('a[href^="#"]'); if (!a) return; const h = a.getAttribute("href"); if (!h || h === "#") return; const t = document.querySelector(h); if (!t) return; e.preventDefault(); closeDrawer(); t.scrollIntoView({ behavior: rdm() ? "auto" : "smooth", block: "start" }); if (history.replaceState) history.replaceState(null, "", h); }); };
  const setupSpy = () => { const secs = secIds.map((id) => document.getElementById(id)).filter((n) => n instanceof HTMLElement); const links = Array.from(document.querySelectorAll("a[data-spy]")); if (!secs.length || !links.length) return; const setA = (id) => links.forEach((l) => l.classList.toggle("is-active", l.getAttribute("href") === `#${id}`)); const spy = new IntersectionObserver((es) => es.forEach((en) => { if (en.isIntersecting) setA(en.target.id); }), { rootMargin: "-45% 0px -45% 0px", threshold: 0 }); secs.forEach((s) => spy.observe(s)); };
  const setupReveal = () => { const nodes = Array.from(document.querySelectorAll("[data-reveal]")); if (!nodes.length) return; if (rdm() || !("IntersectionObserver" in window)) { nodes.forEach((n) => n.classList.add("is-visible")); return; } const ob = new IntersectionObserver((es, o) => es.forEach((en) => { if (en.isIntersecting) { en.target.classList.add("is-visible"); o.unobserve(en.target); } }), { threshold: 0.16 }); nodes.forEach((n) => ob.observe(n)); };
  const setupLazy = () => { const imgs = Array.from(document.querySelectorAll("img[data-src]")); if (!imgs.length) return; const load = (i) => { const s = i.getAttribute("data-src"); if (!s) return; i.src = s; i.removeAttribute("data-src"); }; if (!("IntersectionObserver" in window)) { imgs.forEach(load); return; } const ob = new IntersectionObserver((es, o) => es.forEach((en) => { if (en.isIntersecting) { load(en.target); o.unobserve(en.target); } }), { rootMargin: "120px 0px" }); imgs.forEach((i) => ob.observe(i)); };
  const initCordiantAdaptTooltips = (container) => {
    if (!(container instanceof HTMLElement)) return;
    const wraps = Array.from(container.querySelectorAll("[data-cordiant-adapt-hotspot-wrap]"));
    if (!wraps.length) return;
    const hotspotMap = new Map(CORDIANT_ADAPT_HOTSPOTS.map((item) => [item.id, item]));
    const canHover = window.matchMedia("(hover: hover)").matches;
    let activePinnedId = null;
    let hideTimer = 0;
    const setTooltipVisible = (tooltip, visible) => {
      if (!(tooltip instanceof HTMLElement)) return;
      if (visible) {
        tooltip.hidden = false;
        requestAnimationFrame(() => tooltip.classList.add("is-visible"));
        return;
      }
      tooltip.classList.remove("is-visible");
      window.setTimeout(() => {
        if (!tooltip.classList.contains("is-visible")) tooltip.hidden = true;
      }, 220);
    };
    const positionTooltip = (tooltip) => {
      if (!(tooltip instanceof HTMLElement)) return;
      tooltip.classList.remove("is-shift-left", "is-shift-right");
      const rect = tooltip.getBoundingClientRect();
      const viewportPad = 10;
      if (rect.left < viewportPad) tooltip.classList.add("is-shift-left");
      if (rect.right > window.innerWidth - viewportPad) tooltip.classList.add("is-shift-right");
    };
    const closeAllTooltips = () => {
      activePinnedId = null;
      wraps.forEach((wrap) => {
        const button = wrap.querySelector("[data-cordiant-adapt-hotspot]");
        const tooltip = wrap.querySelector(".cordiant-adapt__tooltip");
        if (button instanceof HTMLElement) {
          button.classList.remove("is-active");
          button.setAttribute("aria-expanded", "false");
        }
        wrap.classList.remove("is-active");
        setTooltipVisible(tooltip, false);
      });
    };
    const showTooltip = (id, pin = false) => {
      const data = hotspotMap.get(id);
      if (!data) return;
      window.clearTimeout(hideTimer);
      wraps.forEach((wrap) => {
        const button = wrap.querySelector("[data-cordiant-adapt-hotspot]");
        const tooltip = wrap.querySelector(".cordiant-adapt__tooltip");
        if (!(button instanceof HTMLButtonElement) || !(tooltip instanceof HTMLElement)) return;
        const currentId = button.getAttribute("data-cordiant-adapt-hotspot");
        const isActive = currentId === id;
        button.classList.toggle("is-active", isActive);
        button.setAttribute("aria-expanded", isActive ? "true" : "false");
        wrap.classList.toggle("is-active", isActive);
        if (isActive) {
          setTooltipVisible(tooltip, true);
          requestAnimationFrame(() => positionTooltip(tooltip));
        } else {
          setTooltipVisible(tooltip, false);
        }
      });
      if (pin) activePinnedId = id;
    };
    const hideTooltip = (id) => {
      wraps.forEach((wrap) => {
        const button = wrap.querySelector("[data-cordiant-adapt-hotspot]");
        const tooltip = wrap.querySelector(".cordiant-adapt__tooltip");
        if (!(button instanceof HTMLButtonElement) || !(tooltip instanceof HTMLElement)) return;
        if (button.getAttribute("data-cordiant-adapt-hotspot") !== id) return;
        button.classList.remove("is-active");
        button.setAttribute("aria-expanded", "false");
        wrap.classList.remove("is-active");
        setTooltipVisible(tooltip, false);
      });
    };
    wraps.forEach((wrap) => {
      const button = wrap.querySelector("[data-cordiant-adapt-hotspot]");
      const tooltip = wrap.querySelector(".cordiant-adapt__tooltip");
      if (!(button instanceof HTMLButtonElement) || !(tooltip instanceof HTMLElement)) return;
      const id = button.getAttribute("data-cordiant-adapt-hotspot");
      if (!id) return;
      const data = hotspotMap.get(id);
      if (!data) return;
      const tooltipId = `cordiant-adapt-tooltip-${id}`;
      button.setAttribute("aria-label", data.label);
      button.setAttribute("aria-describedby", tooltipId);
      button.setAttribute("aria-expanded", "false");
      tooltip.id = tooltipId;
      tooltip.textContent = data.tooltip;
      tooltip.hidden = true;
      if (canHover) {
        wrap.addEventListener("mouseenter", () => {
          if (activePinnedId && activePinnedId !== id) return;
          showTooltip(id, false);
        });
        wrap.addEventListener("mouseleave", () => {
          if (activePinnedId === id) return;
          window.clearTimeout(hideTimer);
          hideTimer = window.setTimeout(() => hideTooltip(id), 80);
        });
      }
      button.addEventListener("focus", () => showTooltip(id, false));
      button.addEventListener("blur", () => {
        window.setTimeout(() => {
          if (activePinnedId === id) return;
          const focused = document.activeElement;
          if (focused instanceof HTMLElement && focused.closest("[data-cordiant-adapt-hotspot-wrap]") === wrap) return;
          hideTooltip(id);
        }, 0);
      });
      button.addEventListener("click", (event) => {
        event.preventDefault();
        if (activePinnedId === id) {
          activePinnedId = null;
          hideTooltip(id);
          return;
        }
        activePinnedId = id;
        showTooltip(id, true);
      });
    });
    document.addEventListener("pointerdown", (event) => {
      const target = event.target;
      if (!(target instanceof Node)) return;
      if (container.contains(target)) return;
      closeAllTooltips();
    });
    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape") closeAllTooltips();
    });
    window.addEventListener("resize", () => {
      if (!activePinnedId) return;
      const activeWrap = wraps.find((wrap) => {
        const button = wrap.querySelector("[data-cordiant-adapt-hotspot]");
        return button instanceof HTMLButtonElement && button.getAttribute("data-cordiant-adapt-hotspot") === activePinnedId;
      });
      const activeTooltip = activeWrap?.querySelector(".cordiant-adapt__tooltip");
      if (activeTooltip instanceof HTMLElement) positionTooltip(activeTooltip);
    });
  };

  const setupCordiantAdapt = () => {
    const container = document.querySelector("[data-cordiant-adapt]");
    if (!(container instanceof HTMLElement)) return;
    initCordiantAdaptTooltips(container);
  };
  const setupMaterialHotspots = () => {
    const points = Array.from(document.querySelectorAll(".hotspot[data-material-target]"));
    const cards = Array.from(document.querySelectorAll(".component-card[data-material-card]"));
    if (!points.length || !cards.length) return;
    const activate = (target) => {
      points.forEach((p) => p.classList.toggle("is-active", p.getAttribute("data-material-target") === target));
      cards.forEach((c) => c.classList.toggle("is-active-material", c.getAttribute("data-material-card") === target));
    };
    points.forEach((p) => p.addEventListener("click", () => {
      const target = p.getAttribute("data-material-target");
      if (!target) return;
      activate(target);
      const card = cards.find((c) => c.getAttribute("data-material-card") === target);
      if (card) card.scrollIntoView({ behavior: rdm() ? "auto" : "smooth", block: "nearest" });
    }));
    activate(points[0].getAttribute("data-material-target"));
  };
  const setupCordiantTechMore = () => {
    const button = document.querySelector("[data-cordiant-tech-more]");
    const extra = document.querySelector("[data-cordiant-tech-meta-extra]");
    const intro = document.querySelector(".cordiant-tech__intro");
    if (!(button instanceof HTMLButtonElement) || !(extra instanceof HTMLElement)) return;
    button.addEventListener("click", () => {
      const isExpanded = button.getAttribute("aria-expanded") === "true";
      const nextExpanded = !isExpanded;
      button.setAttribute("aria-expanded", nextExpanded ? "true" : "false");
      extra.hidden = !nextExpanded;
      if (intro instanceof HTMLElement) intro.classList.toggle("is-meta-expanded", nextExpanded);
    });
  };
  const setupHorizontalArrowNavigation = (viewport, onPrev, onNext, options = {}) => {
    if (!(viewport instanceof HTMLElement)) return;
    const ignoreSelector = typeof options.ignoreSelector === "string" ? options.ignoreSelector.trim() : "";
    viewport.addEventListener("keydown", (event) => {
      const target = event.target;
      if (ignoreSelector && target instanceof Element && target.closest(ignoreSelector)) return;
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        onPrev();
      } else if (event.key === "ArrowRight") {
        event.preventDefault();
        onNext();
      }
    });
  };
  const setupReasonsCarousel = () => {
    const viewport = document.querySelector("[data-reasons-viewport]");
    const track = document.getElementById("reasons-grid");
    const prev = document.querySelector("[data-reasons-prev]");
    const next = document.querySelector("[data-reasons-next]");
    const pagination = document.querySelector("[data-reasons-pagination]");
    if (!(viewport instanceof HTMLElement)
      || !(track instanceof HTMLElement)
      || !(prev instanceof HTMLButtonElement)
      || !(next instanceof HTMLButtonElement)) return;
    const slides = Array.from(track.querySelectorAll(".reason-card"));
    const total = slides.length;
    if (!total) return;
    if (pagination instanceof HTMLElement) {
      pagination.innerHTML = slides.map((_, i) => (
        `<button class="reasons-carousel__dot" type="button" data-reasons-dot="${i}" aria-label="Показать слайд ${i + 1}"></button>`
      )).join("");
    }
    const getSlideWidth = () => {
      const slide = slides[0];
      if (!slide) return 300;
      const gap = parseFloat(getComputedStyle(track).columnGap || "0") || 0;
      return slide.offsetWidth + gap;
    };
    const getCurrentIndex = () => {
      const slideWidth = getSlideWidth();
      return Math.round(viewport.scrollLeft / slideWidth);
    };
    const isAtEnd = () => {
      const tolerance = 2;
      return viewport.scrollLeft + viewport.clientWidth >= viewport.scrollWidth - tolerance;
    };
    const isAtStart = () => {
      return viewport.scrollLeft <= 2;
    };
    const scrollToSlide = (index) => {
      const targetIndex = Math.max(0, Math.min(total - 1, index));
      const slide = slides[targetIndex];
      if (!slide) return;
      viewport.scrollTo({ left: slide.offsetLeft, behavior: "smooth" });
    };
    const syncDots = () => {
      if (!(pagination instanceof HTMLElement)) return;
      const current = getCurrentIndex();
      pagination.querySelectorAll("[data-reasons-dot]").forEach((btn, i) => {
        btn.classList.toggle("is-active", i === current);
        btn.setAttribute("aria-current", i === current ? "true" : "false");
      });
    };
    const syncButtons = () => {
      prev.disabled = false;
      next.disabled = false;
    };
    const moveNext = () => {
      if (isAtEnd()) {
        scrollToSlide(0);
      } else {
        const current = getCurrentIndex();
        scrollToSlide(current + 1);
      }
    };
    const movePrev = () => {
      if (isAtStart()) {
        scrollToSlide(total - 1);
      } else {
        const current = getCurrentIndex();
        scrollToSlide(current - 1);
      }
    };
    prev.addEventListener("click", movePrev);
    next.addEventListener("click", moveNext);
    pagination?.addEventListener("click", (e) => {
      const btn = e.target.closest("[data-reasons-dot]");
      if (!(btn instanceof HTMLButtonElement)) return;
      const i = Number(btn.getAttribute("data-reasons-dot"));
      if (Number.isFinite(i)) scrollToSlide(i);
    });
    setupHorizontalArrowNavigation(viewport, movePrev, moveNext);
    let rafId = 0;
    viewport.addEventListener("scroll", () => {
      cancelAnimationFrame(rafId);
      rafId = requestAnimationFrame(syncDots);
    }, { passive: true });
    syncDots();
    syncButtons();
  };
  const setupCordiantTechSlider = () => {
    const section = document.querySelector(".cordiant-tech");
    const viewport = section?.querySelector("[data-cordiant-tech-viewport]");
    const track = section?.querySelector("[data-cordiant-tech-track]");
    const prev = section?.querySelector("[data-cordiant-tech-prev]");
    const next = section?.querySelector("[data-cordiant-tech-next]");
    const pagination = section?.querySelector("[data-cordiant-tech-pagination]");
    if (!(section instanceof HTMLElement)
      || !(viewport instanceof HTMLElement)
      || !(track instanceof HTMLElement)
      || !(prev instanceof HTMLButtonElement)
      || !(next instanceof HTMLButtonElement)
      || !(pagination instanceof HTMLElement)) return;
    const slides = Array.isArray(CORDIANT_TECH_SLIDES) ? CORDIANT_TECH_SLIDES.slice(0) : [];
    if (!slides.length) return;
    const query = new URLSearchParams(window.location.search);
    const editQuery = (query.get("techHotspotEdit") || "").trim().toLowerCase();
    const isHotspotEditMode = query.has("techHotspotEdit")
      && (editQuery === "" || editQuery === "1" || editQuery === "true" || editQuery === "yes");
    const hotspotModel = isHotspotEditMode
      ? {
        width: CORDIANT_TECH_HOTSPOT_MODEL.width,
        height: CORDIANT_TECH_HOTSPOT_MODEL.height,
        hub: { ...CORDIANT_TECH_HOTSPOT_MODEL.hub },
        items: CORDIANT_TECH_HOTSPOT_MODEL.items.map((item) => ({
          ...item,
          route: Array.isArray(item.route) ? item.route.map((point) => ({ ...point })) : []
        }))
      }
      : CORDIANT_TECH_HOTSPOT_MODEL;
    const hotspotMap = new Map(hotspotModel.items.map((item) => [item.id, item]));
    const resolveHotspotContent = (slideKey, hotspotId) => {
      const bySlide = CORDIANT_TECH_HOTSPOTS_BY_SLIDE?.[slideKey];
      const bySlideItem = bySlide?.[hotspotId];
      const fallback = hotspotMap.get(hotspotId);
      return {
        label: bySlideItem?.label || fallback?.label || "",
        description: bySlideItem?.description || fallback?.description || ""
      };
    };
    const isTouchDevice = !isHotspotEditMode
      && window.matchMedia("(hover: none), (pointer: coarse)").matches;
    const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
    const toPercent = (value, total) => `${((value / total) * 100).toFixed(3)}%`;
    const routeToPoints = (route) => route.map((point) => `${point.x},${point.y}`).join(" ");
    const getHubPercentX = () => toPercent(hotspotModel.hub.x, hotspotModel.width);
    const getHubPercentY = () => toPercent(hotspotModel.hub.y, hotspotModel.height);
    const buildRouteToHub = (x, y) => {
      const routeX = Math.round(clamp((x + hotspotModel.hub.x) / 2, 0, hotspotModel.width));
      return [
        { x: Math.round(x), y: Math.round(y) },
        { x: routeX, y: Math.round(y) },
        { x: routeX, y: hotspotModel.hub.y },
        { x: hotspotModel.hub.x, y: hotspotModel.hub.y }
      ];
    };
    const renderHotspotOverlay = (slideKey) => {
      const lines = hotspotModel.items.map((item) => {
        const points = routeToPoints(item.route);
        return `
          <polyline class="cordiant-tech-overlay-lines__path" data-cordiant-tech-line="${item.id}" points="${points}"></polyline>
        `;
      }).join("");
      const hotspots = hotspotModel.items.map((item) => `
        <button
          class="cordiant-tech-hotspot"
          type="button"
          data-cordiant-tech-hotspot="${item.id}"
          aria-label="${resolveHotspotContent(slideKey, item.id).label}"
          aria-pressed="false"
          style="--cordiant-tech-hotspot-x:${toPercent(item.x, hotspotModel.width)}; --cordiant-tech-hotspot-y:${toPercent(item.y, hotspotModel.height)};"
        >
          <span class="cordiant-tech-hotspot__halo" aria-hidden="true"></span>
          <span class="cordiant-tech-hotspot__core" aria-hidden="true"></span>
        </button>
      `).join("");
      return `
        <div class="cordiant-tech-card__overlay" data-cordiant-tech-overlay style="--cordiant-tech-hub-x:${getHubPercentX()}; --cordiant-tech-hub-y:${getHubPercentY()};">
          <svg class="cordiant-tech-overlay-lines" viewBox="0 0 ${hotspotModel.width} ${hotspotModel.height}" preserveAspectRatio="none" aria-hidden="true">
            ${lines}
          </svg>
          ${hotspots}
          <div class="cordiant-tech-hotspot__hub" data-cordiant-tech-hub aria-hidden="true"></div>
          <div class="cordiant-tech-hotspot__info" data-cordiant-tech-info role="status" aria-live="polite" aria-hidden="true">
            <p class="cordiant-tech-hotspot__info-title" data-cordiant-tech-info-title></p>
            <p class="cordiant-tech-hotspot__info-text" data-cordiant-tech-info-text></p>
          </div>
        </div>
      `;
    };
    const renderSlideCard = (slide, realIndex) => `
      <article class="cordiant-tech-card" data-cordiant-tech-slide="${slide.key}" data-cordiant-tech-real-index="${realIndex}">
        <div class="cordiant-tech-card__media">
          <img class="cordiant-tech-card__tire" src="${slide.image}" alt="Шина ${slide.title}" loading="lazy" />
          ${renderHotspotOverlay(slide.key)}
        </div>
        <div class="cordiant-tech-card__content">
          <h3 class="cordiant-tech-card__title">${slide.title}</h3>
          <p class="cordiant-tech-card__text">${slide.text}</p>
        </div>
        <span class="cordiant-tech-card__watermark" aria-hidden="true">${slide.watermark}</span>
      </article>
    `;
    const hasLoop = slides.length > 1;
    const renderedSlides = hasLoop
      ? [slides[slides.length - 1], ...slides, slides[0]]
      : slides.slice(0);
    track.innerHTML = renderedSlides.map((slide, renderIndex) => {
      const realIndex = hasLoop
        ? (renderIndex === 0 ? slides.length - 1 : (renderIndex === renderedSlides.length - 1 ? 0 : renderIndex - 1))
        : renderIndex;
      return renderSlideCard(slide, realIndex);
    }).join("");
    pagination.innerHTML = slides.map((slide, index) => (
      `<button class="cordiant-tech__pagination-dot" type="button" data-cordiant-tech-dot="${index}" aria-label="Показать слайд ${slide.title}"></button>`
    )).join("");
    const initHotspots = (card) => {
      const media = card.querySelector(".cordiant-tech-card__media");
      const overlay = card.querySelector("[data-cordiant-tech-overlay]");
      const hub = card.querySelector("[data-cordiant-tech-hub]");
      const info = card.querySelector("[data-cordiant-tech-info]");
      const titleNode = card.querySelector(".cordiant-tech-card__title");
      const textNode = card.querySelector(".cordiant-tech-card__text");
      const hotspots = Array.from(card.querySelectorAll("[data-cordiant-tech-hotspot]"));
      const lines = Array.from(card.querySelectorAll("[data-cordiant-tech-line]"));
      if (!(media instanceof HTMLElement)
        || !(overlay instanceof HTMLElement)
        || !(hub instanceof HTMLElement)
        || !(titleNode instanceof HTMLElement)
        || !(textNode instanceof HTMLElement)
        || !hotspots.length
        || !lines.length) return;
      const slideKey = card.getAttribute("data-cordiant-tech-slide") || "";
      const defaultTitle = titleNode.textContent || "";
      const defaultText = textNode.textContent || "";
      if (info instanceof HTMLElement) {
        info.classList.remove("cordiant-tech-hotspot__info--visible");
        info.setAttribute("aria-hidden", "true");
      }
      if (isHotspotEditMode) {
        card.classList.add("cordiant-tech-card--hotspot-edit");
        lines.forEach((line) => {
          line.classList.add("cordiant-tech-overlay-lines__path--base");
        });
        hotspots.forEach((button) => {
          button.setAttribute("aria-pressed", "false");
        });
        return;
      }
      lines.forEach((line) => {
        if (!(line instanceof SVGElement) || typeof line.getTotalLength !== "function") return;
        const length = line.getTotalLength();
        line.style.strokeDasharray = `${length}`;
        line.style.strokeDashoffset = `${length}`;
      });
      let activeHotspotId = null;
      let lockedHotspotId = null;
      const getCurrentHotspotId = () => lockedHotspotId || activeHotspotId;
      const updateCardContent = (hotspotId) => {
        if (!hotspotId) {
          titleNode.textContent = defaultTitle;
          textNode.textContent = defaultText;
          card.classList.remove("cordiant-tech-card--content-hotspot");
          return;
        }
        const hotspot = resolveHotspotContent(slideKey, hotspotId);
        if (!hotspot.label && !hotspot.description) return;
        titleNode.textContent = hotspot.label;
        textNode.textContent = hotspot.description;
        card.classList.add("cordiant-tech-card--content-hotspot");
      };
      const applyHotspotState = () => {
        const currentId = getCurrentHotspotId();
        const hasActive = Boolean(currentId);
        card.classList.toggle("cordiant-tech-card--hotspot-active", hasActive);
        hub.classList.toggle("cordiant-tech-hotspot__hub--active", hasActive);
        hotspots.forEach((button) => {
          const id = button.getAttribute("data-cordiant-tech-hotspot");
          const isActive = id === currentId;
          button.classList.toggle("cordiant-tech-hotspot--active", isActive);
          button.classList.toggle("cordiant-tech-hotspot--dimmed", hasActive && !isActive);
          button.setAttribute("aria-pressed", isActive ? "true" : "false");
        });
        lines.forEach((line) => {
          const id = line.getAttribute("data-cordiant-tech-line");
          const isActive = id === currentId;
          line.classList.toggle("cordiant-tech-overlay-lines__path--active", isActive);
          line.classList.toggle("cordiant-tech-overlay-lines__path--dimmed", hasActive && !isActive);
        });
        updateCardContent(currentId);
      };
      const setActiveHotspot = (id, shouldLock = false) => {
        if (!hotspotMap.has(id)) return;
        activeHotspotId = id;
        if (shouldLock) lockedHotspotId = id;
        applyHotspotState();
      };
      const clearActiveHotspot = () => {
        if (lockedHotspotId) return;
        activeHotspotId = null;
        applyHotspotState();
      };
      const toggleHotspotLock = (id) => {
        if (!hotspotMap.has(id)) return;
        lockedHotspotId = lockedHotspotId === id ? null : id;
        activeHotspotId = lockedHotspotId || id;
        applyHotspotState();
      };
      hotspots.forEach((button) => {
        const id = button.getAttribute("data-cordiant-tech-hotspot");
        if (!id) return;
        button.addEventListener("mouseenter", () => {
          if (isTouchDevice) return;
          setActiveHotspot(id);
        });
        button.addEventListener("focus", () => {
          setActiveHotspot(id);
        });
        button.addEventListener("click", (event) => {
          event.preventDefault();
          if (isTouchDevice) {
            setActiveHotspot(id, true);
            return;
          }
          toggleHotspotLock(id);
        });
        button.addEventListener("keydown", (event) => {
          if (event.key !== "Enter" && event.key !== " ") return;
          event.preventDefault();
          toggleHotspotLock(id);
        });
      });
      media.addEventListener("mouseleave", () => {
        if (isTouchDevice) return;
        clearActiveHotspot();
      });
      overlay.addEventListener("focusout", (event) => {
        const nextFocused = event.relatedTarget;
        if (nextFocused instanceof Node && overlay.contains(nextFocused)) return;
        if (isTouchDevice || lockedHotspotId) return;
        activeHotspotId = null;
        applyHotspotState();
      });
      applyHotspotState();
    };
    track.querySelectorAll(".cordiant-tech-card").forEach((card) => {
      if (!(card instanceof HTMLElement)) return;
      initHotspots(card);
    });
    const getSerializedHotspotModel = () => JSON.stringify({
      width: hotspotModel.width,
      height: hotspotModel.height,
      hub: {
        x: hotspotModel.hub.x,
        y: hotspotModel.hub.y
      },
      items: hotspotModel.items.map((item) => ({
        id: item.id,
        x: Math.round(item.x),
        y: Math.round(item.y),
        route: item.route.map((point) => ({
          x: Math.round(point.x),
          y: Math.round(point.y)
        }))
      }))
    }, null, 2);
    const syncHotspotToDom = (hotspotId) => {
      const hotspot = hotspotMap.get(hotspotId);
      if (!hotspot) return;
      const xPercent = toPercent(hotspot.x, hotspotModel.width);
      const yPercent = toPercent(hotspot.y, hotspotModel.height);
      const routePoints = routeToPoints(hotspot.route);
      const label = `${hotspot.id}: ${Math.round(hotspot.x)}, ${Math.round(hotspot.y)}`;
      track.querySelectorAll(`[data-cordiant-tech-hotspot="${hotspotId}"]`).forEach((button) => {
        if (!(button instanceof HTMLElement)) return;
        button.style.setProperty("--cordiant-tech-hotspot-x", xPercent);
        button.style.setProperty("--cordiant-tech-hotspot-y", yPercent);
        button.setAttribute("data-cordiant-tech-editor-label", label);
      });
      track.querySelectorAll(`[data-cordiant-tech-line="${hotspotId}"]`).forEach((line) => {
        if (!(line instanceof SVGElement)) return;
        line.setAttribute("points", routePoints);
      });
    };
    const syncAllHotspotsToDom = () => {
      hotspotModel.items.forEach((item) => {
        if (!Array.isArray(item.route) || item.route.length < 2) {
          item.route = buildRouteToHub(item.x, item.y);
        }
        syncHotspotToDom(item.id);
      });
      track.querySelectorAll("[data-cordiant-tech-overlay]").forEach((overlay) => {
        if (!(overlay instanceof HTMLElement)) return;
        overlay.style.setProperty("--cordiant-tech-hub-x", getHubPercentX());
        overlay.style.setProperty("--cordiant-tech-hub-y", getHubPercentY());
      });
    };
    const copyTextToClipboard = async (value) => {
      if (navigator.clipboard && typeof navigator.clipboard.writeText === "function") {
        try {
          await navigator.clipboard.writeText(value);
          return true;
        } catch (_error) {
          // fallback below
        }
      }
      const fallback = document.createElement("textarea");
      fallback.value = value;
      fallback.setAttribute("readonly", "");
      fallback.style.position = "fixed";
      fallback.style.opacity = "0";
      document.body.appendChild(fallback);
      fallback.select();
      fallback.setSelectionRange(0, fallback.value.length);
      let copied = false;
      try {
        copied = document.execCommand("copy");
      } catch (_error) {
        copied = false;
      }
      document.body.removeChild(fallback);
      return copied;
    };
    const initHotspotEditor = () => {
      const container = section.querySelector(".cordiant-tech__container");
      if (!(container instanceof HTMLElement)) return;
      section.classList.add("cordiant-tech--hotspot-edit");
      const panel = document.createElement("aside");
      panel.className = "cordiant-tech-hotspot-editor";
      panel.innerHTML = `
        <p class="cordiant-tech-hotspot-editor__lead">
          Режим расстановки хотспотов активен. Перетаскивайте точки и скопируйте JSON с новыми координатами.
        </p>
        <div class="cordiant-tech-hotspot-editor__actions">
          <button class="cordiant-tech-hotspot-editor__copy" type="button" data-cordiant-tech-editor-copy>Скопировать JSON</button>
          <span class="cordiant-tech-hotspot-editor__status" data-cordiant-tech-editor-status aria-live="polite">Параметр: ?techHotspotEdit=1</span>
        </div>
        <pre class="cordiant-tech-hotspot-editor__output" data-cordiant-tech-editor-output></pre>
      `;
      const sliderHead = container.querySelector(".cordiant-tech__slider-head");
      if (sliderHead instanceof HTMLElement) {
        sliderHead.insertAdjacentElement("afterend", panel);
      } else {
        container.prepend(panel);
      }
      const output = panel.querySelector("[data-cordiant-tech-editor-output]");
      const status = panel.querySelector("[data-cordiant-tech-editor-status]");
      const copyButton = panel.querySelector("[data-cordiant-tech-editor-copy]");
      const renderEditorOutput = () => {
        if (!(output instanceof HTMLElement)) return;
        output.textContent = getSerializedHotspotModel();
      };
      const updateStatus = (text) => {
        if (!(status instanceof HTMLElement)) return;
        status.textContent = text;
      };
      const setHotspotPointFromClient = (hotspotId, overlay, clientX, clientY) => {
        const hotspot = hotspotMap.get(hotspotId);
        if (!hotspot) return;
        const rect = overlay.getBoundingClientRect();
        if (!rect.width || !rect.height) return;
        const nextX = Math.round(clamp(((clientX - rect.left) / rect.width) * hotspotModel.width, 0, hotspotModel.width));
        const nextY = Math.round(clamp(((clientY - rect.top) / rect.height) * hotspotModel.height, 0, hotspotModel.height));
        if (hotspot.x === nextX && hotspot.y === nextY) return;
        hotspot.x = nextX;
        hotspot.y = nextY;
        hotspot.route = buildRouteToHub(nextX, nextY);
        syncHotspotToDom(hotspotId);
        renderEditorOutput();
      };
      let dragState = null;
      const stopDrag = (event) => {
        if (!dragState || event.pointerId !== dragState.pointerId) return;
        if (dragState.button instanceof HTMLElement) {
          dragState.button.classList.remove("is-dragging");
          if (typeof dragState.button.releasePointerCapture === "function") {
            try {
              dragState.button.releasePointerCapture(event.pointerId);
            } catch (_error) {
              // ignore
            }
          }
        }
        section.classList.remove("is-hotspot-dragging");
        dragState = null;
      };
      track.addEventListener("pointerdown", (event) => {
        const target = event.target;
        if (!(target instanceof Element)) return;
        const button = target.closest("[data-cordiant-tech-hotspot]");
        if (!(button instanceof HTMLButtonElement)) return;
        if (!event.isPrimary) return;
        if (event.pointerType === "mouse" && event.button !== 0) return;
        const hotspotId = button.getAttribute("data-cordiant-tech-hotspot");
        const overlay = button.closest("[data-cordiant-tech-overlay]");
        if (!hotspotId || !(overlay instanceof HTMLElement)) return;
        event.preventDefault();
        event.stopPropagation();
        dragState = {
          pointerId: event.pointerId,
          hotspotId,
          overlay,
          button
        };
        button.classList.add("is-dragging");
        if (typeof button.setPointerCapture === "function") {
          try {
            button.setPointerCapture(event.pointerId);
          } catch (_error) {
            // ignore
          }
        }
        section.classList.add("is-hotspot-dragging");
        setHotspotPointFromClient(hotspotId, overlay, event.clientX, event.clientY);
      });
      document.addEventListener("pointermove", (event) => {
        if (!dragState || event.pointerId !== dragState.pointerId) return;
        event.preventDefault();
        setHotspotPointFromClient(dragState.hotspotId, dragState.overlay, event.clientX, event.clientY);
      }, { passive: false });
      document.addEventListener("pointerup", stopDrag);
      document.addEventListener("pointercancel", stopDrag);
      if (copyButton instanceof HTMLButtonElement) {
        copyButton.addEventListener("click", async () => {
          const text = getSerializedHotspotModel();
          const copied = await copyTextToClipboard(text);
          updateStatus(copied ? "JSON скопирован в буфер обмена." : "Не удалось скопировать автоматически.");
        });
      }
      syncAllHotspotsToDom();
      renderEditorOutput();
      updateStatus("Параметр: ?techHotspotEdit=1");
    };
    let activeRenderIndex = hasLoop ? 1 : 0;
    const getGap = () => {
      const style = window.getComputedStyle(track);
      return parseFloat(style.columnGap || style.gap || "0") || 0;
    };
    const getStep = () => {
      const card = track.querySelector(".cordiant-tech-card");
      if (!(card instanceof HTMLElement)) return viewport.clientWidth;
      return card.getBoundingClientRect().width + getGap();
    };
    const getRealIndex = () => {
      if (!hasLoop) return activeRenderIndex;
      if (activeRenderIndex === 0) return slides.length - 1;
      if (activeRenderIndex === renderedSlides.length - 1) return 0;
      return activeRenderIndex - 1;
    };
    const sync = (animate = true) => {
      track.style.transition = animate && !rdm() ? "transform 450ms ease" : "none";
      const shift = -(getStep() * activeRenderIndex);
      track.style.transform = `translateX(${shift}px)`;
      if (hasLoop) {
        prev.disabled = false;
        next.disabled = false;
      } else {
        prev.disabled = activeRenderIndex <= 0;
        next.disabled = activeRenderIndex >= slides.length - 1;
      }
      const realIndex = getRealIndex();
      pagination.querySelectorAll("[data-cordiant-tech-dot]").forEach((button, index) => {
        const isActive = index === realIndex;
        button.classList.toggle("is-active", isActive);
        button.setAttribute("aria-current", isActive ? "true" : "false");
      });
    };
    const normalizeLoopPosition = () => {
      if (!hasLoop) return;
      if (activeRenderIndex === 0) {
        activeRenderIndex = slides.length;
        sync(false);
      } else if (activeRenderIndex === renderedSlides.length - 1) {
        activeRenderIndex = 1;
        sync(false);
      }
    };
    const moveBy = (direction) => {
      if (hasLoop) {
        activeRenderIndex += direction;
        sync(true);
        return;
      }
      const target = Math.min(slides.length - 1, Math.max(0, activeRenderIndex + direction));
      if (target === activeRenderIndex) {
        sync(false);
        return;
      }
      activeRenderIndex = target;
      sync(true);
    };
    const goToRealIndex = (realIndex) => {
      const target = Math.min(slides.length - 1, Math.max(0, realIndex));
      activeRenderIndex = hasLoop ? target + 1 : target;
      sync(true);
    };
    prev.addEventListener("click", () => moveBy(-1));
    next.addEventListener("click", () => moveBy(1));
    pagination.addEventListener("click", (event) => {
      const button = event.target.closest("[data-cordiant-tech-dot]");
      if (!(button instanceof HTMLButtonElement)) return;
      const index = Number(button.getAttribute("data-cordiant-tech-dot"));
      if (!Number.isFinite(index)) return;
      goToRealIndex(index);
    });
    setupHorizontalArrowNavigation(viewport, () => moveBy(-1), () => moveBy(1), {
      ignoreSelector: "[data-cordiant-tech-hotspot]"
    });
    track.addEventListener("transitionend", (event) => {
      if (event.propertyName !== "transform") return;
      normalizeLoopPosition();
    });
    window.addEventListener("resize", () => sync(false));
    sync(false);
    if (isHotspotEditMode) {
      initHotspotEditor();
    }
  };
  const initCordiantAccordion = (container, itemsConfig = CORDIANT_ACCORDION_ITEMS, defaultIndex = CORDIANT_ACCORDION_DEFAULT_INDEX) => {
    if (!(container instanceof HTMLElement)) return;
    const carouselRoot = container.querySelector("[data-cordiant-accordion-carousel-root]");
    const viewport = container.querySelector("[data-cordiant-accordion-carousel-viewport]");
    const track = container.querySelector("[data-cordiant-accordion-carousel-track]");
    const prev = container.querySelector("[data-cordiant-accordion-carousel-prev]");
    const next = container.querySelector("[data-cordiant-accordion-carousel-next]");
    const pagination = container.querySelector("[data-cordiant-accordion-carousel-pagination]");
    const nav = carouselRoot instanceof HTMLElement ? carouselRoot.querySelector(".cordiant-accordion-carousel__nav") : null;
    if (!(viewport instanceof HTMLElement)
      || !(track instanceof HTMLElement)
      || !(prev instanceof HTMLButtonElement)
      || !(next instanceof HTMLButtonElement)
      || !(pagination instanceof HTMLElement)) return;
    const items = Array.isArray(itemsConfig) ? itemsConfig.filter((item) => item && typeof item.title === "string") : [];
    if (!items.length) return;
    const escapeHtml = (value) => String(value).replace(/[&<>"']/g, (char) => ({
      "&": "&amp;",
      "<": "&lt;",
      ">": "&gt;",
      "\"": "&quot;",
      "'": "&#39;"
    }[char] || char));
    const sectionId = container.getAttribute("id") || "section";
    const idPrefix = `cordiant-accordion-carousel-${sectionId}`;
    const renderCard = (item, realIndex, domKey) => {
      const paragraphs = Array.isArray(item.paragraphs) ? item.paragraphs : [];
      const textHtml = paragraphs
        .map((paragraph, paragraphIndex) => `<p class="cordiant-accordion-carousel__text ${paragraphIndex === 0 ? "cordiant-accordion-carousel__text--primary" : "cordiant-accordion-carousel__text--secondary"}">${escapeHtml(paragraph)}</p>`)
        .join("");
      const mediaHtml = typeof item.image === "string" && item.image
        ? `<figure class="cordiant-accordion-carousel__media"><img class="cordiant-accordion-carousel__image" src="${escapeHtml(item.image)}" alt="${escapeHtml(item.title)}" loading="lazy" /></figure>`
        : `<figure class="cordiant-accordion-carousel__media cordiant-accordion-carousel__media--placeholder" aria-hidden="true"></figure>`;
      return `
        <article class="cordiant-accordion-carousel__card" data-cordiant-accordion-carousel-card="${realIndex}">
          ${mediaHtml}
          <div class="cordiant-accordion-carousel__content">
            <h3 class="cordiant-accordion-carousel__title">${escapeHtml(item.title)}</h3>
            <div class="cordiant-accordion-carousel__scroll">${textHtml}</div>
          </div>
        </article>
      `;
    };
    const hasLoop = items.length > 1;
    const cloneCount = hasLoop ? Math.min(2, items.length) : 0;
    const renderedSlides = hasLoop
      ? [
        ...items.slice(-cloneCount).map((item, i) => ({ item, realIndex: items.length - cloneCount + i, domKey: `clone-start-${i}` })),
        ...items.map((item, i) => ({ item, realIndex: i, domKey: `real-${i}` })),
        ...items.slice(0, cloneCount).map((item, i) => ({ item, realIndex: i, domKey: `clone-end-${i}` }))
      ]
      : items.map((item, i) => ({ item, realIndex: i, domKey: `real-${i}` }));
    track.innerHTML = renderedSlides.map((slide) => renderCard(slide.item, slide.realIndex, slide.domKey)).join("");
    let cardNodes = Array.from(track.querySelectorAll("[data-cordiant-accordion-carousel-card]"));
    const getVisibleCount = () => {
      if (window.innerWidth >= 768) return 2;
      return 1;
    };
    let visibleCount = getVisibleCount();
    let activeRenderIndex = hasLoop
      ? cloneCount + Math.min(items.length - 1, Math.max(0, Number(defaultIndex) || 0))
      : Math.min(Math.max(0, Number(defaultIndex) || 0), Math.max(0, items.length - visibleCount));
    const getMaxIndex = () => Math.max(0, items.length - visibleCount);
    const getRealIndex = () => {
      const card = cardNodes[activeRenderIndex];
      if (!(card instanceof HTMLElement)) return 0;
      const raw = Number(card.getAttribute("data-cordiant-accordion-carousel-card"));
      return Number.isFinite(raw) ? raw : 0;
    };
    const getOffsetForIndex = (index) => {
      const card = cardNodes[index];
      if (!(card instanceof HTMLElement)) return 0;
      return card.offsetLeft;
    };
    const updateNavPosition = () => {
      if (!(carouselRoot instanceof HTMLElement) || !(nav instanceof HTMLElement)) return;
      const activeCard = cardNodes[activeRenderIndex];
      if (!(activeCard instanceof HTMLElement)) return;
      const activeMedia = activeCard.querySelector(".cordiant-accordion-carousel__media");
      if (!(activeMedia instanceof HTMLElement)) return;
      const rootRect = carouselRoot.getBoundingClientRect();
      const viewportRect = viewport.getBoundingClientRect();
      const mediaRect = activeMedia.getBoundingClientRect();
      const navWidth = nav.offsetWidth;
      const navHeight = nav.offsetHeight;
      const navX = Math.round((viewportRect.left - rootRect.left) + mediaRect.width - navWidth);
      const navY = Math.round((viewportRect.top - rootRect.top) - navHeight);
      carouselRoot.style.setProperty("--cordiant-accordion-carousel-nav-x", `${Math.max(0, navX)}px`);
      carouselRoot.style.setProperty("--cordiant-accordion-carousel-nav-y", `${navY}px`);
    };
    const scheduleNavPositionUpdate = () => {
      window.requestAnimationFrame(() => {
        updateNavPosition();
        window.requestAnimationFrame(updateNavPosition);
        window.setTimeout(updateNavPosition, 430);
      });
    };
    const renderPagination = () => {
      const pages = hasLoop ? items.length : (getMaxIndex() + 1);
      pagination.innerHTML = Array.from({ length: pages }, (_, index) => (
        `<button class="cordiant-accordion-carousel__dot" type="button" data-cordiant-accordion-carousel-dot="${index}" aria-label="Слайд ${index + 1}"></button>`
      )).join("");
    };
    const sync = (animate = true) => {
      visibleCount = getVisibleCount();
      if (!hasLoop) {
        activeRenderIndex = Math.min(activeRenderIndex, getMaxIndex());
      }
      cardNodes.forEach((card, index) => {
        if (!(card instanceof HTMLElement)) return;
        const isActiveLeft = index === activeRenderIndex;
        card.classList.toggle("is-active-left", isActiveLeft);
      });
      track.style.transition = animate && !rdm() ? "transform 420ms ease" : "none";
      track.style.transform = `translateX(${-getOffsetForIndex(activeRenderIndex)}px)`;
      if (hasLoop) {
        prev.disabled = false;
        next.disabled = false;
      } else {
        prev.disabled = activeRenderIndex <= 0;
        next.disabled = activeRenderIndex >= getMaxIndex();
      }
      const activeRealIndex = getRealIndex();
      pagination.querySelectorAll("[data-cordiant-accordion-carousel-dot]").forEach((button, index) => {
        const isActive = index === activeRealIndex;
        button.classList.toggle("is-active", isActive);
        button.setAttribute("aria-current", isActive ? "true" : "false");
      });
      scheduleNavPositionUpdate();
    };
    const moveBy = (delta) => {
      if (hasLoop) {
        activeRenderIndex += delta;
        sync(true);
        return;
      }
      const target = Math.max(0, Math.min(getMaxIndex(), activeRenderIndex + delta));
      if (target === activeRenderIndex) {
        sync(false);
        return;
      }
      activeRenderIndex = target;
      sync(true);
    };
    prev.addEventListener("click", () => moveBy(-1));
    next.addEventListener("click", () => moveBy(1));
    pagination.addEventListener("click", (event) => {
      const button = event.target.closest("[data-cordiant-accordion-carousel-dot]");
      if (!(button instanceof HTMLButtonElement)) return;
      const index = Number(button.getAttribute("data-cordiant-accordion-carousel-dot"));
      if (!Number.isFinite(index)) return;
      if (hasLoop) {
        activeRenderIndex = cloneCount + Math.max(0, Math.min(items.length - 1, index));
      } else {
        activeRenderIndex = Math.max(0, Math.min(getMaxIndex(), index));
      }
      sync(true);
    });
    setupHorizontalArrowNavigation(viewport, () => moveBy(-1), () => moveBy(1));
    track.addEventListener("transitionend", (event) => {
      if (event.propertyName !== "transform" || !hasLoop) return;
      if (activeRenderIndex < cloneCount) {
        activeRenderIndex = cloneCount + items.length - 1;
        sync(false);
      } else if (activeRenderIndex >= cloneCount + items.length) {
        activeRenderIndex = cloneCount;
        sync(false);
      }
    });
    track.addEventListener("load", (event) => {
      const target = event.target;
      if (!(target instanceof HTMLElement) || !target.classList.contains("cordiant-accordion-carousel__image")) return;
      scheduleNavPositionUpdate();
    }, true);
    window.addEventListener("resize", () => {
      cardNodes = Array.from(track.querySelectorAll("[data-cordiant-accordion-carousel-card]"));
      renderPagination();
      sync(false);
    }, { passive: true });
    renderPagination();
    sync(false);
    setTimeout(() => sync(false), 360);
  };
  const setupCordiantAccordion = () => {
    document.querySelectorAll(".cordiant-accordion").forEach((container) => {
      initCordiantAccordion(container);
    });
  };
  const setupQuizProgressUi = async () => {
    const quizCard = document.querySelector("#quiz .quiz-card");
    if (!quizCard) return;

    const quizMain = quizCard.querySelector(".quiz-layout__main");
    const pageButtons = Array.from(quizCard.querySelectorAll(".quiz-page-list .quiz-page-num"));
    const prevButton = quizCard.querySelector(".quiz-page-btn:not(.quiz-page-btn--next)");
    const nextButton = quizCard.querySelector(".quiz-page-btn--next");
    const resultStepButton = quizCard.querySelector("[data-quiz-result-step]");
    const resultStepLottieContainer = quizCard.querySelector("[data-quiz-lock-lottie]");
    const questionBlock = quizCard.querySelector(".quiz-question");
    const questionIndex = quizCard.querySelector(".quiz-question__index");
    const questionTitle = quizCard.querySelector(".quiz-question__title");
    const questionVisualImage = quizCard.querySelector(".quiz-layout__visual img");
    const optionsBlock = quizCard.querySelector(".quiz-options");
    const answerButtons = Array.from(quizCard.querySelectorAll(".quiz-option"));
    const pagination = quizCard.querySelector(".quiz-pagination");
    const feedbackBlock = quizCard.querySelector("[data-quiz-feedback]");
    const feedbackStatus = quizCard.querySelector("[data-quiz-feedback-status]");
    const feedbackText = quizCard.querySelector("[data-quiz-feedback-text]");
    const feedbackRetryButton = quizCard.querySelector("[data-quiz-feedback-retry]");
    const feedbackRetryText = quizCard.querySelector("[data-quiz-feedback-retry-text]");
    const feedbackRetryRefreshIcon = quizCard.querySelector(".quiz-feedback__icon-refresh");
    const feedbackRetryNextIcon = quizCard.querySelector(".quiz-feedback__icon-next");
    const resultBlock = quizCard.querySelector("[data-quiz-result]");
    const resultClaimFormWrap = quizCard.querySelector("[data-quiz-claim-form-wrap]");
    const resultClaimForm = quizCard.querySelector("[data-quiz-claim-form]");
    const resultClaimError = quizCard.querySelector("[data-quiz-claim-error]");
    const resultClaimSuccess = quizCard.querySelector("[data-quiz-claim-success]");
    const progressRing = quizCard.querySelector("[data-quiz-progress]");
    const progressValue = quizCard.querySelector("[data-quiz-progress-value]");

    if (!pageButtons.length || !progressRing || !progressValue || !answerButtons.length) return;

    const createDefaultQuizConfig = () => ({
      codeWord: "SUMMER",
      labels: {
        correct: "Да, верно!",
        wrong: "Нет, неверно."
      },
      resultSummaries: {
        high: "Отличный результат. Вы уверенно ориентируетесь в базовых параметрах подбора шин.",
        medium: "Хороший результат. Вы знаете основные принципы, но есть точки для усиления.",
        low: "Базовый результат. Рекомендуем еще раз пройти квиз и освежить ключевые правила выбора шин."
      },
      questions: [
        {
          question: "Какая модель летних шин Cordiant обеспечивает экономию топлива до 30% по сравнению с предшественником?",
          paginationTitle: "Какая модель летних шин Cordiant обеспечивает экономию топлива до 30% по сравнению с предшественником?",
          options: ["Cordiant Gravity", "Cordiant Run Tour", "Cordiant Off Road 2"],
          correct: 1,
          fact: "Оптимизированный состав резиновой смеси и облегченный брекерный пакет в модели Cordiant Run Tour способствуют снижению сопротивления качению и экономии топлива до 30% по сравнению с шиной-предшественником.",
          image: {
            src: "assets/img/quiz/1.jpg",
            alt: "Шина Cordiant Run Tour"
          }
        }
      ]
    });

    const normalizeQuizConfig = (rawConfig) => {
      const fallbackConfig = createDefaultQuizConfig();

      if (!rawConfig || typeof rawConfig !== "object") {
        return fallbackConfig;
      }

      const fallbackQuestions = fallbackConfig.questions;
      const sourceQuestions = Array.isArray(rawConfig.questions) ? rawConfig.questions : [];
      const normalizedQuestions = sourceQuestions
        .map((entry, index) => {
          if (!entry || typeof entry !== "object") return null;

          const fallbackQuestion = fallbackQuestions[index] || fallbackQuestions[0];
          const question = typeof entry.question === "string" ? entry.question.trim() : "";
          const options = Array.isArray(entry.options)
            ? entry.options
                .filter((option) => typeof option === "string")
                .map((option) => option.trim())
                .filter(Boolean)
            : [];
          const correct = Number.isInteger(entry.correct) ? entry.correct : -1;

          if (!question || options.length < 2 || correct < 0 || correct >= options.length) {
            return null;
          }

          const legacyFactSource =
            typeof entry.feedbackCorrect === "string" && entry.feedbackCorrect.trim()
              ? entry.feedbackCorrect.trim()
              : typeof entry.feedbackWrong === "string" && entry.feedbackWrong.trim()
                ? entry.feedbackWrong.trim()
                : "";
          const baseFact =
            typeof entry.fact === "string" && entry.fact.trim()
              ? entry.fact.trim()
              : legacyFactSource || fallbackQuestion.fact;

          const sourceImage = entry.image && typeof entry.image === "object" ? entry.image : {};
          const imageSrc =
            typeof sourceImage.src === "string" && sourceImage.src.trim()
              ? sourceImage.src.trim()
              : fallbackQuestion.image.src;
          const imageAlt =
            typeof sourceImage.alt === "string" && sourceImage.alt.trim()
              ? sourceImage.alt.trim()
              : fallbackQuestion.image.alt;

          return {
            question,
            paginationTitle:
              typeof entry.paginationTitle === "string" && entry.paginationTitle.trim()
                ? entry.paginationTitle.trim()
                : question,
            options,
            correct,
            fact: baseFact,
            image: {
              src: imageSrc,
              alt: imageAlt
            }
          };
        })
        .filter(Boolean);

      const sourceResultSummaries =
        rawConfig.resultSummaries && typeof rawConfig.resultSummaries === "object" ? rawConfig.resultSummaries : {};
      const sourceLabels = rawConfig.labels && typeof rawConfig.labels === "object" ? rawConfig.labels : {};

      const resultSummaries = {
        high:
          typeof sourceResultSummaries.high === "string" && sourceResultSummaries.high.trim()
            ? sourceResultSummaries.high.trim()
            : fallbackConfig.resultSummaries.high,
        medium:
          typeof sourceResultSummaries.medium === "string" && sourceResultSummaries.medium.trim()
            ? sourceResultSummaries.medium.trim()
            : fallbackConfig.resultSummaries.medium,
        low:
          typeof sourceResultSummaries.low === "string" && sourceResultSummaries.low.trim()
            ? sourceResultSummaries.low.trim()
            : fallbackConfig.resultSummaries.low
      };

      const labels = {
        correct:
          typeof sourceLabels.correct === "string" && sourceLabels.correct.trim()
            ? sourceLabels.correct.trim()
            : fallbackConfig.labels.correct,
        wrong:
          typeof sourceLabels.wrong === "string" && sourceLabels.wrong.trim()
            ? sourceLabels.wrong.trim()
            : fallbackConfig.labels.wrong
      };

      return {
        codeWord: typeof rawConfig.codeWord === "string" && rawConfig.codeWord.trim() ? rawConfig.codeWord.trim() : fallbackConfig.codeWord,
        labels,
        resultSummaries,
        questions: normalizedQuestions.length ? normalizedQuestions : fallbackQuestions
      };
    };

    const loadQuizConfig = async () => {
      if (window.location.protocol === "file:") {
        console.warn("[quiz] Страница открыта через file://. Загрузка JSON может быть заблокирована браузером.");
      }
      try {
        const response = await fetch("assets/data/quiz-data.json", { cache: "no-store" });
        if (!response.ok) {
          throw new Error(`HTTP ${response.status}`);
        }
        const rawConfig = await response.json();
        return normalizeQuizConfig(rawConfig);
      } catch (error) {
        console.warn("[quiz] Не удалось загрузить assets/data/quiz-data.json, используются встроенные данные.", error);
        return normalizeQuizConfig(createDefaultQuizConfig());
      }
    };

    const quizConfig = await loadQuizConfig();
    const quizData = quizConfig.questions;
    const feedbackStatusLabels = quizConfig.labels;

    const totalQuestions = Math.min(pageButtons.length, quizData.length);
    if (!totalQuestions) return;
    const selectedAnswers = Array(totalQuestions).fill(-1);

    let activeIndex = Math.max(0, pageButtons.findIndex((button) => button.classList.contains("is-active")));
    if (activeIndex < 0) activeIndex = 0;
    if (activeIndex >= totalQuestions) activeIndex = totalQuestions - 1;
    let isResultStep = false;
    let isFeedbackStep = false;
    let lockedQuizMainHeight = 0;
    let isResultStepLocked = true;
    let lockLottieAnimation = null;
    let hasResultUnlockAnimationPlayed = false;
    let hasResultClaimBeenSubmitted = false;

    const normalizeImageSrc = (src) => {
      if (!src || typeof src !== "string") return "assets/img/quiz/1.jpg";
      if (src.startsWith("http://") || src.startsWith("https://") || src.startsWith("data:")) return src;
      return src.startsWith("/") ? src.slice(1) : src;
    };

    const lockQuizMainHeight = () => {
      if (!(quizMain instanceof HTMLElement)) return;
      const currentHeight = Math.ceil(quizMain.getBoundingClientRect().height);
      if (currentHeight <= 0) return;
      if (currentHeight > lockedQuizMainHeight) {
        lockedQuizMainHeight = currentHeight;
      }
      quizMain.style.minHeight = `${lockedQuizMainHeight}px`;
    };

    const scheduleQuizMainHeightLock = () => {
      window.requestAnimationFrame(lockQuizMainHeight);
    };

    const applyResultLockAnimationState = (isLocked) => {
      if (!lockLottieAnimation) return;

      const lastFrame = Math.max(0, Math.floor(lockLottieAnimation.totalFrames || 1) - 1);

      if (isLocked) {
        hasResultUnlockAnimationPlayed = false;
        lockLottieAnimation.stop();
        lockLottieAnimation.goToAndStop(0, true);
        return;
      }

      if (rdm()) {
        hasResultUnlockAnimationPlayed = true;
        lockLottieAnimation.stop();
        lockLottieAnimation.goToAndStop(lastFrame, true);
        return;
      }

      if (!hasResultUnlockAnimationPlayed) {
        hasResultUnlockAnimationPlayed = true;
        lockLottieAnimation.goToAndPlay(0, true);
        return;
      }

      lockLottieAnimation.stop();
      lockLottieAnimation.goToAndStop(lastFrame, true);
    };

    const initResultLockLottie = () => {
      if (!(resultStepLottieContainer instanceof HTMLElement)) return;
      if (!window.lottie || typeof window.lottie.loadAnimation !== "function") return;

      const animationConfig = {
        container: resultStepLottieContainer,
        renderer: "svg",
        loop: false,
        autoplay: false,
        rendererSettings: {
          preserveAspectRatio: "xMidYMid meet",
          progressiveLoad: true
        }
      };

      if (window.quizOpenLockData && typeof window.quizOpenLockData === "object") {
        animationConfig.animationData = window.quizOpenLockData;
      } else {
        animationConfig.path = "assets/img/quiz/open-lock.json";
      }

      lockLottieAnimation = window.lottie.loadAnimation(animationConfig);

      lockLottieAnimation.addEventListener("DOMLoaded", () => {
        resultStepLottieContainer.classList.add("is-lottie-ready");
        applyResultLockAnimationState(isResultStepLocked);
      });

      lockLottieAnimation.addEventListener("data_failed", () => {
        resultStepLottieContainer.classList.remove("is-lottie-ready");
      });
    };

    if (pagination instanceof HTMLElement) {
      pagination.style.setProperty("--quiz-pagination-columns", String(totalQuestions + 3));
    }

    const isQuizCompleted = () => selectedAnswers.every((answer) => answer !== -1);
    const getAnsweredCount = () => selectedAnswers.reduce((acc, answer) => acc + (answer !== -1 ? 1 : 0), 0);

    const updateProgressRing = () => {
      const percent = Math.round((getAnsweredCount() / totalQuestions) * 100);
      progressRing.style.setProperty("--progress", String(percent));
      progressRing.setAttribute("aria-valuenow", String(percent));
      progressValue.textContent = `${percent}%`;
    };

    const updateNavButtons = () => {
      if (prevButton instanceof HTMLButtonElement) {
        prevButton.disabled = !isResultStep && !isFeedbackStep && activeIndex === 0;
      }

      if (!(nextButton instanceof HTMLButtonElement)) return;

      if (isResultStep) {
        nextButton.disabled = true;
        nextButton.textContent = "›";
        nextButton.setAttribute("aria-label", "Следующий шаг");
        return;
      }

      if (isFeedbackStep) {
        const isLastQuestion = activeIndex === totalQuestions - 1;
        if (isLastQuestion) {
          nextButton.disabled = true;
          nextButton.textContent = "›";
          nextButton.setAttribute("aria-label", "Следующий вопрос недоступен");
          return;
        }

        nextButton.disabled = false;
        nextButton.textContent = "›";
        nextButton.setAttribute("aria-label", "Следующий вопрос");
        return;
      }

      if (selectedAnswers[activeIndex] === -1) {
        nextButton.disabled = true;
        nextButton.textContent = "›";
        nextButton.setAttribute("aria-label", "Ответьте на вопрос");
      } else {
        nextButton.disabled = false;
        nextButton.textContent = "›";
        nextButton.setAttribute("aria-label", "Показать проверку ответа");
      }
    };

    const renderPagination = () => {
      const isCompleted = isQuizCompleted();

      pageButtons.forEach((button, index) => {
        const inRange = index < totalQuestions;
        button.hidden = !inRange;
        if (!inRange) return;

        const answer = selectedAnswers[index];
        const isCorrect = answer !== -1 && answer === quizData[index].correct;
        const isWrong = answer !== -1 && answer !== quizData[index].correct;

        button.classList.toggle("is-correct", isCorrect);
        button.classList.toggle("is-wrong", isWrong);
        button.classList.toggle("is-active", index === activeIndex && !isResultStep);

        if (index === activeIndex && !isResultStep) {
          button.setAttribute("aria-current", "page");
        } else {
          button.removeAttribute("aria-current");
        }
      });

      if (resultStepButton instanceof HTMLButtonElement) {
        const isLocked = !isCompleted;
        isResultStepLocked = isLocked;
        resultStepButton.classList.toggle("is-locked", isLocked);
        resultStepButton.classList.toggle("is-active", isResultStep);
        resultStepButton.disabled = isLocked;
        resultStepButton.setAttribute("aria-disabled", String(isLocked));
        applyResultLockAnimationState(isLocked);
      }

      updateNavButtons();
    };

    const renderQuestionVisual = () => {
      if (!(questionVisualImage instanceof HTMLImageElement)) return;
      const item = quizData[activeIndex];
      const fallbackSrc = "assets/img/quiz/1.jpg";
      const imageSrc = normalizeImageSrc(item?.image?.src || fallbackSrc);
      const imageAlt = typeof item?.image?.alt === "string" && item.image.alt.trim() ? item.image.alt.trim() : "Изображение вопроса квиза";
      questionVisualImage.setAttribute("src", imageSrc);
      questionVisualImage.setAttribute("alt", imageAlt);
    };

    const renderAnswerOptions = () => {
      const item = quizData[activeIndex];
      if (!item || !item.options || !answerButtons.length) return;

      if (selectedAnswers[activeIndex] >= item.options.length) {
        selectedAnswers[activeIndex] = -1;
      }
      const answeredIndex = selectedAnswers[activeIndex];

      answerButtons.forEach((button, optionIndex) => {
        const textSlot = button.querySelector("span:not(.quiz-option__letter)");
        const inRange = optionIndex < item.options.length;
        button.hidden = !inRange;
        button.style.display = inRange ? "" : "none";

        if (!inRange) {
          button.classList.remove("is-selected");
          button.disabled = true;
          button.setAttribute("aria-pressed", "false");
          if (textSlot) {
            textSlot.textContent = "";
          } else {
            button.textContent = "";
          }
          return;
        }

        const isSelectedOption = optionIndex === answeredIndex;
        const text = item.options[optionIndex] || "";

        button.classList.remove("is-selected");
        button.classList.toggle("is-selected", isSelectedOption);
        button.disabled = false;
        button.setAttribute("aria-pressed", isSelectedOption ? "true" : "false");

        if (textSlot) {
          textSlot.textContent = text;
        } else {
          button.textContent = text;
        }
      });
    };

    const renderFeedback = () => {
      const item = quizData[activeIndex];
      const selected = selectedAnswers[activeIndex];
      if (!item || selected === -1) return;

      const isCorrect = selected === item.correct;
      if (feedbackStatus) {
        feedbackStatus.textContent = isCorrect ? feedbackStatusLabels.correct : feedbackStatusLabels.wrong;
        feedbackStatus.classList.toggle("is-good", isCorrect);
        feedbackStatus.classList.toggle("is-bad", !isCorrect);
      }

      if (feedbackText) {
        feedbackText.textContent = item.fact || "";
      }

      if (feedbackRetryButton instanceof HTMLButtonElement) {
        feedbackRetryButton.classList.remove("is-hidden");

        if (isCorrect) {
          const isLastQuestion = activeIndex === totalQuestions - 1;
          const nextLabel = isLastQuestion ? "Смотреть результаты" : "Следующий вопрос";

          feedbackRetryButton.dataset.action = isLastQuestion ? "results" : "next";
          feedbackRetryButton.setAttribute("aria-label", nextLabel);
          feedbackRetryButton.setAttribute("title", nextLabel);
          if (feedbackRetryText) feedbackRetryText.textContent = nextLabel;
          if (feedbackRetryRefreshIcon) feedbackRetryRefreshIcon.classList.add("is-hidden");
          if (feedbackRetryNextIcon) feedbackRetryNextIcon.classList.remove("is-hidden");
        } else {
          feedbackRetryButton.dataset.action = "retry";
          feedbackRetryButton.setAttribute("aria-label", "Изменить ответ");
          feedbackRetryButton.setAttribute("title", "Изменить ответ");
          if (feedbackRetryText) feedbackRetryText.textContent = "Изменить ответ";
          if (feedbackRetryRefreshIcon) feedbackRetryRefreshIcon.classList.remove("is-hidden");
          if (feedbackRetryNextIcon) feedbackRetryNextIcon.classList.add("is-hidden");
        }
      }
    };

    const getQuizResultStats = () => {
      const correctAnswers = selectedAnswers.reduce((acc, value, index) => {
        const correct = quizData[index] ? quizData[index].correct : -1;
        return acc + (value === correct ? 1 : 0);
      }, 0);
      const wrongAnswers = totalQuestions - correctAnswers;
      const resultAccuracy = totalQuestions > 0 ? Math.round((correctAnswers / totalQuestions) * 100) : 0;

      return {
        correctAnswers,
        wrongAnswers,
        totalQuestions,
        resultAccuracy
      };
    };

    const syncResultClaimState = () => {
      if (resultClaimFormWrap instanceof HTMLElement) {
        resultClaimFormWrap.hidden = hasResultClaimBeenSubmitted;
      }

      if (resultClaimError instanceof HTMLElement) {
        resultClaimError.hidden = true;
        resultClaimError.textContent = "";
      }

      if (resultClaimSuccess instanceof HTMLElement) {
        resultClaimSuccess.hidden = !hasResultClaimBeenSubmitted;
      }
    };

    const showQuestionStep = () => {
      isResultStep = false;
      isFeedbackStep = false;
      quizCard.classList.remove("is-result-step");
      quizCard.classList.remove("is-feedback-step");

      if (questionBlock instanceof HTMLElement) questionBlock.hidden = false;
      if (optionsBlock instanceof HTMLElement) optionsBlock.hidden = false;
      if (feedbackBlock instanceof HTMLElement) feedbackBlock.hidden = true;
      if (pagination instanceof HTMLElement) pagination.hidden = false;
      if (resultBlock instanceof HTMLElement) resultBlock.hidden = true;

      if (questionIndex) {
        questionIndex.textContent = `Вопрос ${activeIndex + 1} из ${totalQuestions}`;
      }

      if (questionTitle) {
        const item = quizData[activeIndex];
        questionTitle.textContent = item ? item.question : "";
      }

      renderQuestionVisual();
      renderAnswerOptions();
      updateProgressRing();
      renderPagination();
      scheduleQuizMainHeightLock();
    };

    const showFeedbackStep = () => {
      const selected = selectedAnswers[activeIndex];
      if (selected === -1) return;

      isResultStep = false;
      isFeedbackStep = true;
      quizCard.classList.remove("is-result-step");
      quizCard.classList.add("is-feedback-step");

      if (questionBlock instanceof HTMLElement) questionBlock.hidden = false;
      if (optionsBlock instanceof HTMLElement) optionsBlock.hidden = true;
      if (feedbackBlock instanceof HTMLElement) feedbackBlock.hidden = false;
      if (pagination instanceof HTMLElement) pagination.hidden = false;
      if (resultBlock instanceof HTMLElement) resultBlock.hidden = true;

      renderQuestionVisual();
      renderFeedback();
      updateProgressRing();
      renderPagination();
      scheduleQuizMainHeightLock();
    };

    const showResultStep = () => {
      if (!isQuizCompleted()) return;

      isResultStep = true;
      isFeedbackStep = false;
      quizCard.classList.add("is-result-step");
      quizCard.classList.remove("is-feedback-step");

      if (questionBlock instanceof HTMLElement) questionBlock.hidden = true;
      if (optionsBlock instanceof HTMLElement) optionsBlock.hidden = true;
      if (feedbackBlock instanceof HTMLElement) feedbackBlock.hidden = true;
      if (pagination instanceof HTMLElement) pagination.hidden = false;
      if (resultBlock instanceof HTMLElement) resultBlock.hidden = false;

      syncResultClaimState();

      updateProgressRing();
      renderPagination();
      scheduleQuizMainHeightLock();
    };

    pageButtons.forEach((button, index) => {
      if (index >= totalQuestions) {
        button.hidden = true;
        return;
      }

      const item = quizData[index];
      if (item) {
        const title = item.paginationTitle || item.question;
        button.title = title;
        button.setAttribute("aria-label", `Вопрос ${index + 1}: ${title}`);
        button.textContent = String(index + 1);
      }

      button.addEventListener("click", () => {
        activeIndex = index;
        showQuestionStep();
      });
    });

    if (resultStepButton instanceof HTMLButtonElement) {
      resultStepButton.addEventListener("click", () => {
        if (resultStepButton.disabled) return;
        showResultStep();
      });
    }

    if (prevButton instanceof HTMLButtonElement) {
      prevButton.addEventListener("click", () => {
        if (isResultStep) {
          activeIndex = totalQuestions - 1;
          showFeedbackStep();
          return;
        }

        if (isFeedbackStep) {
          showQuestionStep();
          return;
        }

        activeIndex = Math.max(0, activeIndex - 1);
        showQuestionStep();
      });
    }

    if (nextButton instanceof HTMLButtonElement) {
      nextButton.addEventListener("click", () => {
        if (isResultStep) return;

        if (isFeedbackStep) {
          if (activeIndex === totalQuestions - 1) {
            return;
          }

          activeIndex = Math.min(totalQuestions - 1, activeIndex + 1);
          showQuestionStep();
          return;
        }

        if (selectedAnswers[activeIndex] === -1) return;
        showFeedbackStep();
      });
    }

    answerButtons.forEach((button, optionIndex) => {
      button.addEventListener("click", () => {
        if (isResultStep || isFeedbackStep) return;
        selectedAnswers[activeIndex] = optionIndex;
        showFeedbackStep();
      });
    });

    if (feedbackRetryButton instanceof HTMLButtonElement) {
      feedbackRetryButton.addEventListener("click", () => {
        const action = feedbackRetryButton.dataset.action || "retry";

        if (action === "results") {
          if (isQuizCompleted()) showResultStep();
          return;
        }

        if (action === "next") {
          if (activeIndex < totalQuestions - 1) {
            activeIndex += 1;
            showQuestionStep();
            return;
          }
          if (isQuizCompleted()) {
            showResultStep();
            return;
          }
        }

        selectedAnswers[activeIndex] = -1;
        showQuestionStep();
        const firstOption = answerButtons[0];
        if (firstOption instanceof HTMLElement) firstOption.focus();
      });
    }

    if (resultClaimForm instanceof HTMLFormElement) {
      resultClaimForm.addEventListener("submit", async (event) => {
        event.preventDefault();
        if (!resultClaimForm.reportValidity()) return;

        if (resultClaimError instanceof HTMLElement) {
          resultClaimError.hidden = true;
          resultClaimError.textContent = "";
        }

        const submitButton = resultClaimForm.querySelector(".quiz-result__submit");
        if (submitButton instanceof HTMLButtonElement) {
          submitButton.disabled = true;
          submitButton.textContent = "Отправка...";
        }

        const formData = new FormData(resultClaimForm);
        const fullName = String(formData.get("fullName") || "").trim();
        const phone = String(formData.get("phone") || "").trim();
        const email = String(formData.get("email") || "").trim();
        const stats = getQuizResultStats();

        try {
          const response = await fetch("save-quiz-result.php", {
            method: "POST",
            headers: {
              "Content-Type": "application/json"
            },
            body: JSON.stringify({
              fullName,
              phone,
              email,
              correctAnswers: stats.correctAnswers,
              wrongAnswers: stats.wrongAnswers,
              totalQuestions: stats.totalQuestions,
              resultAccuracy: stats.resultAccuracy,
              source: "podium-quiz"
            })
          });

          let payload = null;
          try {
            payload = await response.json();
          } catch {
            payload = null;
          }

          if (!response.ok || !payload || payload.ok !== true) {
            throw new Error(payload && payload.message ? payload.message : "Ошибка отправки");
          }

          hasResultClaimBeenSubmitted = true;
          syncResultClaimState();
          resultClaimForm.reset();
          scheduleQuizMainHeightLock();
        } catch (error) {
          console.warn("[quiz] Не удалось сохранить результат формы.", error);
          if (resultClaimError instanceof HTMLElement) {
            resultClaimError.textContent = "Не удалось отправить данные. Попробуйте еще раз.";
            resultClaimError.hidden = false;
          }
        } finally {
          if (submitButton instanceof HTMLButtonElement) {
            submitButton.disabled = false;
            submitButton.textContent = "Отправить";
          }
        }
      });
    }

    window.addEventListener("resize", () => {
      if (!(quizMain instanceof HTMLElement)) return;
      quizMain.style.minHeight = "";
      lockedQuizMainHeight = 0;
      scheduleQuizMainHeightLock();
    });

    initResultLockLottie();
    showQuestionStep();
  };
  const setupCordiantHero = () => {
    const quizButton = document.querySelector(".cordiant-hero__quiz");
    if (!(quizButton instanceof HTMLButtonElement)) return;
    quizButton.addEventListener("click", () => {
      const target = document.getElementById("quiz") || document.getElementById("reasons");
      if (!(target instanceof HTMLElement)) return;
      target.scrollIntoView({ behavior: rdm() ? "auto" : "smooth", block: "start" });
      const focusTarget = target.querySelector("h2, h3, button, [tabindex]");
      if (focusTarget instanceof HTMLElement) {
        if (!focusTarget.hasAttribute("tabindex") && !/^(BUTTON|A|INPUT|SELECT|TEXTAREA)$/.test(focusTarget.tagName)) {
          focusTarget.setAttribute("tabindex", "-1");
        }
        window.setTimeout(() => focusTarget.focus({ preventScroll: true }), rdm() ? 0 : 350);
      }
    });
  };
  const setupUi = () => { if (toggle) toggle.addEventListener("click", openDrawer); if (closeBtn) closeBtn.addEventListener("click", () => closeDrawer()); document.querySelectorAll(".mobile-nav a").forEach((a) => a.addEventListener("click", () => closeDrawer())); document.addEventListener("keydown", (e) => { if (e.key === "Escape" && drawer && drawer.classList.contains("is-open")) closeDrawer(); }); window.addEventListener("resize", () => { if (window.innerWidth > 1024) closeDrawer(true); updHeader(); }); window.addEventListener("scroll", updHeader, { passive: true }); };
  const fill = () => { setText("hero-title", C.hero.title); setText("hero-subtitle", C.hero.subtitle); setText("hero-lead", C.hero.lead); setText("brand-intro", C.brand.intro); setText("brand-symbolism", C.brand.symbolism); setText("brand-testing", C.brand.testing); setText("brand-rnd", C.brand.rnd); setText("brand-lab", C.brand.lab); setText("brand-advantages", C.brand.advantages); setText("summer-overview", C.summerOverview); setText("tech-stages", C.tech.stages); setText("tech-sites", C.tech.sites); setText("tech-staff", C.tech.staff); setText("tech-quality", C.tech.quality); setText("tech-strategy-main", C.tech.strategyMain); setText("tech-strategy-extra", C.tech.strategyExtra); setText("tech-development", C.tech.development); setText("russia-intro", C.russia.intro); setText("russia-details", C.russia.details); setText("materials-intro", C.materials.intro); };
  fill();
  renderTimeline();
  renderBrandStoryPanels();
  renderModels();
  renderPanels("summer-feature-panels", "summer-feature", C.summerFeatured);
  renderPanels("model-tech-panels", "model-tech", {
    runTour: {
      title: "Технологии шин Cordiant Run Tour",
      text: C.modelTech.runTour,
      image: modelTechMeta.runTour.image,
      tags: modelTechMeta.runTour.tech
    },
    gravity: {
      title: "Технологии шин Cordiant Gravity",
      text: C.modelTech.gravity,
      image: modelTechMeta.gravity.image,
      tags: modelTechMeta.gravity.tech
    },
    gravitySuv: {
      title: "Технологии шин Cordiant Gravity SUV",
      text: C.modelTech.gravitySuv,
      image: modelTechMeta.gravitySuv.image,
      tags: modelTechMeta.gravitySuv.tech
    },
    comfort2: {
      title: "Технологии шин Cordiant Comfort 2",
      text: C.modelTech.comfort2,
      image: modelTechMeta.comfort2.image,
      tags: modelTechMeta.comfort2.tech
    }
  });
  renderLogic();
  renderMaterials();
  renderReasons();
  renderSummerPodium();
  setupTabs();
  setupMilestonesMore();
  setupAnchor();
  setupSpy();
  setupReveal();
  setupLazy();
  setupMaterialHotspots();
  setupCordiantTechMore();
  setupCordiantAdapt();
  setupReasonsCarousel();
  setupCordiantTechSlider();
  setupCordiantAccordion();
  setupQuizProgressUi();
  setupCordiantHero();
  setupUi();
  updHeader();
})();
